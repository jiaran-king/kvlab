# KVLab Ascend v3.1 四 P／4K 原生对账

2026-09-24。范围是最终 v3.1 配置的 10／16／24 GiB 每 rank P 缓存预算；下文所有“吻合”均限于这套已冻结输入、路由、顺序和 P producer 协议。模拟未执行模型，也未预测延迟、吞吐或 AFD 的显存节省。

## 依据与实现

- 固定实测为 `cal10-v31-0924-01`、`cal16-v31-0924-01`、`cal24-v31-0924-01`；`formal01-v31-0924-01` 是独立 16 GiB 重复。四轮 1,313 条实际输入、P 归属及每 P 顺序一致，按输入 SHA 和归档索引校验。冻结输入 32,361,644 token；P producer 删除每请求末尾一个 `128822` 后，P 实际处理输入 32,360,331 token。原生查询分母仍为 32,361,644。后续输入采用每轮已捕获的 token IDs，不由模拟输出生成。
- 目标 P 的 vLLM commit 是 `568afb3a13806beb53bb2e6bd518269357b237c0`，vllm-ascend commit 是 `3281a5fc44ec344ba304c9161a3959d8649471f4`。目标 P 启动日志和归档核实了原生调度、缓存源码及无本地 diff；锁定的 Scheduler 与关键缓存源码摘要吻合。原生六组布局、44 个 KV 张量和 812,416 字节／物理块，给出 13,216／21,146／31,719 块。四个 P 为独立缓存域，每个 P 最多一请求在途，MBT 8192、seqs 2、scheduler block 32、hash block 2、retention／合法恢复粒度 4096。
- `kvsim.native.run_ascend_v31` 调用目标 `AsyncScheduler`、`KVCacheManager` 和 `AscendHybridKVCacheCoordinator`。P request 先按完整捕获 token 查询，producer 再截末尾 token；完成 P prefill 后等待 transfer 完成并释放引用。P 启动日志明确启用异步调度；回放按目标 `EngineCore.step_with_batch_queue` 先排两批、再处理最旧输出。只把真实输出长度留作冻结输入元数据，不执行 D 侧生成。
- 初版同步单批推进在 10 GiB P0 的第 17 条请求错误采用 20,480 token，实测为 0；总采用量 6,615,040，等于实测 16 GiB P0，而实测 10 GiB P0 是 3,403,776。仅把类名改为 `AsyncScheduler` 仍有此偏差；补上两批在途顺序后，P0 326 条逐请求完全对齐。该适配由目标引擎源码与 P 日志支持，没有使用实测命中量参与调度决策。

## 三档完整结果

每档均独立重建四个 P 的缓存池和冷起点，运行各 P 完整历史。以下四个模拟计数与实测总量相同，逐请求差额也全部为 0。

| P 预算／rank | 物理块／rank | P 输入 | 实际采用／原生命中 | local-compute | 原生查询 | 采用比例 | 原生查询命中率 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 10 GiB | 13,216 | 32,360,331 | 12,079,104 | 20,281,227 | 32,361,644 | 37.3269% | 37.3254% |
| 16 GiB | 21,146 | 32,360,331 | 24,948,736 | 7,411,595 | 32,361,644 | 77.0967% | 77.0935% |
| 24 GiB | 31,719 | 32,360,331 | 25,518,080 | 6,842,251 | 32,361,644 | 78.8561% | 78.8529% |

各 P 的采用量（模拟＝实测）：

| 预算 | P0 | P1 | P2 | P3 |
|---:|---:|---:|---:|---:|
| 10 GiB | 3,403,776 | 2,457,600 | 2,912,256 | 3,305,472 |
| 16 GiB | 6,615,040 | 5,652,480 | 5,853,184 | 6,828,032 |
| 24 GiB | 6,782,976 | 5,799,936 | 5,947,392 | 6,987,776 |

10→16 GiB 增加 12,869,632 adopted token、减少同量 local-compute，涉及 537 个请求／64 个 session；16→24 GiB 增加 569,344，涉及 22 个请求／22 个 session。模拟与实测的受益请求集合相同。总计 3,939 条模拟请求记录均满足采用量为 4,096 的整数倍、每条 `adopted + local_compute = P input`、查询数 `= P input + 1`，没有抢占；每条都有 P producer transfer 完成和引用释放。独立 16 GiB 重复轮的 1,313 条逐请求实测和全局／各 P 计数与校准轮完全相同。

机器可读汇总在 `validation/ascend-v31/summary.json`；完整逐请求比较在 `validation/ascend-v31/v31-native-comparison.json`。四个 P 的冻结输入、12 份原生输出、原始冻结正文归档及四轮实测事件与审计文件均在同目录，见其 README。实测参考文件只在运行之后由比较器读取。

## 回归与使用边界

G1 缓存 contrast 内嵌摘要比率已在原有 bundle／validator 路径核验，错误比率反例和合法组合纳入回归。随包 native 测试 42 项通过。短链 A(8,500)→B(10,000)→A 续接在目标原生协调器上采用 0／0／8192，最后本地处理 308 token；完整历史同时覆盖四 P 隔离、4K 恢复和 producer 引用释放。实测没有抢占，因此本轮不能验证 Ascend 抢占恢复语义。CPU-only 运行环境仍需目标 Python／vLLM／vllm-ascend 依赖，不能用任意新版本代替；常规后处理不需要 NPU。详细入口见 `START_HERE.md`。

这证明在目标 v3.1 条件下，“每 rank 有效 KV 容量 → 前缀采用、原生查询命中与 P local-compute”这一环可复算。后续比较 AFD／non-AFD 还需分别取得可核验的每 rank KV 可用预算及其 tensor/layout／保留块映射，并固定或显式声明两侧的模型、输入、路由和推进协议。此报告没有计算 AFD 本身的显存占用，也没有证明 AFD 相对 non-AFD 的收益。
