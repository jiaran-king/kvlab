# Codex Goal：KVLab 可靠性收尾与 Agent 直接使用

## 1. 目标和边界

在当前实现上修复验收的 F1–F5，使一个具备文件读写、命令执行能力的外部 Agent，能够：

**理解用户场景 → 配置实验 → 调用模拟器 → 核验结果 → 整理指标与证据。**

研究范围仍是 **prefix-cache 实际采用比例、原生查询命中率和 local-compute**。不预测 TTFT／吞吐，不增加硬件后端。

继续使用现有原生 Scheduler、缓存管理、CLI 和 JSON/JSONL。\*\*不开发 Agent 框架、项目内 LLM 调用、自然语言解析器、MCP/HTTP 服务或新的调度层。\*\*HTML 暂缓，已有页面保留为可选查看器，不作为本轮验收条件。

先核对实际工作区；已经修好且有证据的项目直接复用，保留现有有效结果，不重新实施上一版全部功能。

## 2. 修复五项验收问题

以 `KVLab_cache_delivery_review_20260924.md` 及其复现材料为依据。先定位实际文件；附件不可用时，以已读取的“修复比较器缺口”对话中的 F1–F5 说明和反例为依据，不因缺少报告文件而阻塞可开展的修复。

优先在现有模块内修复，不新建通用校验框架。

| 项目最小修复要求回归标准      |                                                                                     |                                                                 |
| ----------------- | ----------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| **F1：部署比较漏检机制差异** | 所有比较模式检查锁定源码与固定缓存规则；部署对照只允许已支持、已声明的预算、拓扑、路由和调度差异，并列明实际变化。                           | 源码／retention 反例被拒绝；合法 MBT、seqs、拓扑变化仍能比较，不能一律要求 effective 全字段相同。 |
| **F2：基线与证据错配**    | Contrast 两侧均绑定实际运行；comparison、evidence 同样核对 run、workload 和比较对。同 workload 不能替代同 run。 | 错误基线组合必须失败；正确组合保持通过。旧记录身份不充分时明确拒绝附加组合。                          |
| **F3：多 P 半轮中断**   | Complete 仍严格要求完整轮次；incomplete 允许符合记录语义的最后部分轮次，单列观测边界，不补假零。                          | 一个半轮失败点保留状态，另外两个完整可比点仍能比较；不能顺便放过中间缺行或损坏。                        |
| **F4：比率缺少公式核验**   | 在现有 validator 中核验全局和各 P 的查询命中率、实际采用比例与分子分母，处理零分母、浮点容差和非法非有限值。                       | `hits=0` 却报告 75% 的反例被拒绝；合法零命中、空 P 和 NA 不误报。                     |
| **F5：独立包测试和复跑入口** | 当前测试使用随包小 fixture，历史测试与依赖边界分清；修正生成报告中的旧条件文件路径。                                      | 干净包的指定测试和文档命令可执行，不靠大规模补入旧工作区，也不用跳过掩盖必需资产缺失。                     |

F3 的记账边界必须明确：\*\*最后半轮中已经成功完成的 P 工作仍须计入；未执行或失败批次不得计入。\*\*部分轮次的观测单列，不能作为完整轮次的全局峰值，也不能丢弃该轮已经完成的工作。

本轮以修复和回归为主，不重复开展全面审计。共享必要校验，避免 compare、contrast、bundle 各自维护一套不一致规则。**F1/F2 必须在代码中阻止，不能只在 Agent 说明里写“请检查”。**

已有正常运行的输入、采用量和 local-compute 应保持不变；元数据和报告格式变化单列说明。确有旧计数错误需要修正时，解释依据，不为保持数字掩盖问题。

## 3. Agent 适配只做三件事

### A. 把现有命令写清楚

在同一份 `START_HERE` 增加或完善“Agent 使用”一节，不重复建设多份 README、AGENTS 和 skill。

说明支持的 profile、参数及单位、随包 workload／route／conditions 入口、环境依赖，以及实际执行顺序：

**选择或构造受支持输入 → 冻结条件 → 校验 → 运行 → 比较与证据 → 读取结果。**

提供一份可复制修改的场景示例，命令必须来自最终代码的实际执行。Agent 可以编辑配置、构造符合现有格式的小负载，但不应为日常使用修改模拟核心。内容身份、依赖和假设来源必须保留。

\*\*优先直接使用现有命令。\*\*只有实际试用发现明显重复操作时，才补一个调用现有函数的薄入口，不为了“Agent 化”强制重做 CLI。

### B. 让现有结构化结果容易读取

复用 manifest、summary、comparison/contrast、requests、events 和 evidence，**不新建内容重复的&#x20;****`agent-result`****&#x20;格式，不重命名已经清楚的字段。**

Agent 应能从现有文件读清楚：

| 信息最低要求 |                                                      |
| ------ | ---------------------------------------------------- |
| 状态     | 是否完成、是否可比、失败或未完成原因、完成／计划请求数。                         |
| 实验身份   | 实际 run、有效配置、workload/routing 身份和预算来源。                |
| 主结果    | 实际采用量与比例、原生 query/hit 与命中率、累计 local-compute、可用对照的差值。 |
| 证据入口   | 对应比较对、P、request ID，以及明细和事件文件位置。                      |

实际采用比例与查询命中率分开，缺失与零分开。成功生成文件不等于模拟完成，incomplete 不混入完整负载收益，不可比不能解释成零收益。

非法输入、校验失败返回非零退出码；incomplete 保留状态、产物路径，并写清退出码语义。**不需要把所有第三方日志改造成 JSON：命令结束给出状态和结果路径，Agent 读取经过校验的文件即可。**

默认先读小摘要，必要时再定位请求与事件；不要把完整 token 和事件流全部塞进摘要。

### C. 规定解释顺序，不实现 Agent 本身

使用说明中约定，Agent 的回答包含：

**实际条件与假设 → 主指标及差值 → 关键请求证据 → 结论边界 → 配置和结果位置。**

标准汇总与校验由工具完成，Agent 负责组织解释，不要求每次使用都临时重写计数脚本。

参数缺失时，优先采用有来源的默认值并列明；会改变实验意图的关键条件无法判断时再确认。不支持的 profile、缺少真实预算要如实说明，不能静默换模型、改容量、缩并发或不断修改场景追求正收益。

已有结果重分析与新执行要分清。本轮只交付这段使用约定，不开发自动提示词优化、多 Agent 编排或项目内模型调用。

## 4. 最小验收：修复回归＋一次使用演练

\*\*先跑 F1–F5 反例和合法正例。\*\*不能靠一律拒绝来通过测试。保留 history、active、two\_p 的正常后处理链路，确认核心数字不漂移。

然后在干净目录进行一次使用演练：优先让**没有当前长对话上下文的 Agent**，仅看到说明、样例资产和自然语言任务，完成一组 **3–8 请求、两个预算点**的原生 CPU 小对照，再整理结果。可以复用已有历史竞争 fixture，不另造大负载。

同时用已有 active 结果检查它能否如实说明“两个主指标没有差异”，再给一个错配或不支持的输入，检查它会不会报告工具拒绝原因，而不是擅自改变实验。半轮 incomplete 由 F3 回归覆盖，不为每种状态另跑原生实验。

留下配置、命令、退出状态、run ID、摘要与关键证据。验收重点是：**不改核心、不依赖 HTML、实际场景符合要求、回答数字与文件一致。**

\*\*修复回归与 Agent 原生端到端演练分别报告状态。\*\*环境无法提供独立 Agent 或原生运行时，分别标注“手工按文档验证”“后处理验证”或具体阻塞；这些验证不能替代 Agent 原生端到端演练的完成声明，也不为演练新增外部服务。

纯后处理、校验和文档修改不要求重跑 76／425 全量；只有修改驱动落盘或执行记账边界时，补一次有界的对应异常路径测试。沿用现有 CPU/SLURM 授权、服务器健康规则和资源限制。

## 5. 交付与停止条件

交付更新后的一个源码／样例包，更新同一份 `START_HERE` 和验收报告。报告列明：

- F1–F5 的修复状态及实际测试结果。
- 正常后处理链路的结果是否保持一致。
- Agent 使用演练的执行方式、结果和证据。
- 工具流程与真实 AFD 场景各自的验证范围。
- 尚未完成或受环境限制的项目。

按以下顺序推进：

**修复并回归 → 补说明和必要输出缺口 → 小型使用演练 → 干净包交付。**

完成标准是：**五项问题被正确处理，合法路径保持可用；Agent 能按照说明调用工具，并从结构化结果给出有依据的结论。**

完成所需验证受阻时，保留已完成工作并报告具体缺口，不将部分验证包装为全部完成。达到标准后结束，不继续扩展实验矩阵、前端、性能模型或 Agent 平台。