# Goal：四张 H20 上的 Agent Replay 前缀缓存容量收益验证

## 1. 最终目标

在**同节点四张 H20、DeepSeek-V4-Flash、原生 P/D 分离**的条件下，沿用已有多轮 Trace Replay，**只改变 Prefill 侧 KV 缓存容量**，以尽量少的回放回答：

> 增加 P 侧容量，是否增加 P 本地前缀复用、减少本地输入计算，并降低后续轮请求的 TTFT？

自主完成必要环境核验、部署、短 smoke、H/L 对照、结果汇总和资源清理，不停留在方案或脚本阶段。

\*\*验收要求是取得有效、可解释的容量对照，不是必须得到正收益。\*\*先跑 H/L 两次，能回答问题就结束，最多四次正式回放。不接入 AFD，不将结果解释为 AFD 的实际加速比。

## 2. 固定边界

| 项目约定   |                                                           |
| ------ | --------------------------------------------------------- |
| GPU    | 同一节点，总计四张 H20，不自动增加 GPU 或跨节点                              |
| 部署     | 一个 P 实例 TP2＋一个 D 实例 TP2，即 1P1D                            |
| 模型     | 服务器已有 DeepSeek-V4-Flash，不更换 checkpoint 或量化方案              |
| 运行时    | 优先复用现有 vLLM 0.26.0 环境，记录实际版本及修改                           |
| 实验变量   | 只改变 P 侧实际 KV 缓存预算                                         |
| D 侧    | 配置、KV 预算固定；本地 APC 默认关闭                                    |
| KV 传输  | 固定一套兼容的节点内 P→D connector，不启用 D→P 跨轮回读或额外 KV offload       |
| 正式负载   | 三个完整 session，共 174 条记录，不裁剪、不复制扩容、不重新挑选                    |
| 回放规则   | 三个 session 交错；`max_concurrency=3`、seed=0、同 actor 间隔缩放 0.1 |
| 输入输出   | 保留 trace 长度设置和既有上下文重建逻辑，不强制 output=1                      |
| 正式回放预算 | 先 H/L 两次；第三、第四次按具体问题决定；最多四次                               |

正式负载使用 AgentInfer 固定 commit：
```
7f304555a8bc504c2c2257db2662e896eeeb5c30
```

源文件：
```
tests/agentbench/replay/claude_trace_8session_requests.jsonl
```

选择以下完整 session，保留全部原始字段：

| 完整 session ID记录数                       |     |
| -------------------------------------- | --- |
| `66fd0455-0715-4b7b-ab93-d2aeb4323268` | 65  |
| `c4d23e13-7f6c-4998-af37-d6a3e8b4067f` | 75  |
| `7aefcc2d-844d-4445-b575-350f065a0a52` | 34  |
| 合计                                     | 174 |

这是**等待时间缩放后的 synthetic Trace Replay**，不是真实工具执行或原始生产时序的完整复现。

## 3. 复用入口与资源约束

优先检查并复用：
```swift
工作目录：
/home/david_cwq/zhouziheng/agent-replay-20260917

模型目录：
/home/david_cwq/jinghaoyu/models/DeepSeek-V4-Flash

设计文档候选：
EXPERIMENT_DESIGN_MINIMAL_PD.md
```

这些是查找入口，不代表已验证可运行。manifest 或设计文件不存在时，可直接按上述完整 ID 从固定版本 trace 提取负载，核对总数，不另建 manifest 系统。

通过 `ssh vllm-h20-head` 协调，并通过 SLURM 申请同节点四张 GPU；计算、模型加载和实验均在有效分配内执行。不得取消他人作业、修改共享模型或覆盖他人环境。

提交前设置明确的作业时限、启动超时、Replay 超时和有限重试。资源不足时不重复提交；资源等待不计入正式回放次数。遵守全局服务器健康、进程归属及清理规则，包括已有 H20 reconnect 清理例外。

## 4. 最小部署与 smoke

按 P=TP2、D=TP2 部署，优先复用已有 proxy、connector、启动和 Replay 脚本。

`max_model_len` 覆盖固定 trace 的输入加输出需求；已知最大值为 75,768 token，可从 `81920` 起步。调度候选可采用：
```java
P max_num_seqs = 2
D max_num_seqs = 4
max_num_batched_tokens = 8192
```

这些只是启动候选，不进行最优参数搜索。允许必要的小修；核心路径不兼容时保留阻塞证据，不扩展为新 connector、缓存机制或内核开发。

使用约 3～5 条请求，尽量合并验证：

1. 请求真实经过 P prefill、KV 交付和 D 生成，没有回退为 D 完整 prefill。
2. 同一续接链第二轮出现可观测的 **P 本地 APC 命中**。
3. 接近正式负载最大上下文的请求可以完成，输入输出未被静默截断。
4. Replay 推理、tokenizer 相关端点可用，客户端能正确采集 TTFT；指标入口能区分 P、D。

D APC 若因既有兼容路径必须开启，记录并固定设置，明确区分 P 本地与 D/外部命中；不能区分则不进入正式回放。

smoke 不计入正式回放，但不能借此运行完整 trace、进行容量扫描或长时间调优。

## 5. 容量选点与配置冻结

**H：高容量参考点。**

以 P 侧 `gpu_memory_utilization=0.90` 作为启动候选，依据实际分配确定 H。记录每张 P GPU 的 KV 字节预算、实际分配和现有缓存分组信息。

H 不是无淘汰基准，不保证保住全部历史，也不代表物理显存上限。

**L：明显较小、仍满足既定上下文要求的容量。**

从约 H/4 一类粗档选取，根据启动信息调整；H/8 仅为必要时的候选。不建立精确工作集模拟器，不做二分容量搜索。完整 L 回放本身就是压力检查。

使用当前运行时支持的显式 KV 容量控制方式，确认 H/L 实际分配不同。不得同时改变上下文上限、调度参数或 D 预算，也不能将两张 TP 卡的物理字节简单换算为双倍逻辑 token 容量。

\*\*完成 smoke 和容量选点后，在 R1 前冻结所有非容量配置。\*\*若正式回放开始后必须修改影响性能的配置，修改前后的结果不得组成同一组 H/L 对照；既有正式运行仍计入次数预算。

## 6. 正式执行与停止条件

执行顺序为 **R1=H、R2=L**。

每档采用相同的初始化与测量流程：

1. 使用相同的少量非正式请求完成必要预热，不使用完整正式负载。
2. 等待请求和 KV 传输结束，确认 P 前缀缓存为空。
3. 记录 metrics 起始快照。
4. 运行固定的 174 条记录。
5. 保存全部请求结果、终止快照和日志。

\*\*重置 metrics 不等于清缓存。\*\*H 的首次正式回放也不能继承 smoke 前缀。优先使用已验证的清缓存接口；无法确认清空时重启 P，并重新执行上述流程。

容量切换需要重启 P 时，仅在现有 connector 支持重新连接的情况下保留 D；否则整组重启。D 配置保持不变，不开发新重连机制。

H/L 后立即决策：

| 观察结果下一步              |                     |
| -------------------- | ------------------- |
| 命中增加、计算减少、TTFT 明显改善  | 可以结束；中间点有决策价值时再补 M  |
| L 太宽裕或容量过低           | 最多修正 L 一次，替代 M      |
| 命中增加、计算减少，但 TTFT 没改善 | 可以结束，报告复用未转化为明显延迟收益 |
| 差异小且无明确配置问题          | 按已测范围不敏感或证据不足收尾     |
| 缺少有效对照且无法在边界内修复      | 报告部分完成及具体阻塞         |

M 可取有效 L/H 实际预算的近似几何中点，按运行时要求取整。

第四次仅用于验证一个明确修正或解释具体异常，执行前记录它要解决的问题。\*\*任何按完整 174 条负载启动的正式回放，即使中途失败，都计入最多四次预算。\*\*不为凑三点或追求正收益追加运行。

## 7. 测量与解释

优先使用现有 Replay 输出、metrics 和日志，仅补充必要的轻量采集、汇总代码。

| 指标口径     |                                                   |
| -------- | ------------------------------------------------- |
| P 实际容量   | 每卡 KV 预算及实际分配；已有 block/group 信息一并保存               |
| P 本地命中率  | 测量窗口内本地命中 token 增量／查询 token 增量，避免 D/外部命中和 TP 重复计数 |
| P 本地输入计算 | 优先直接计数；推导值标为估算或未命中量，不能冒充物理计算量                     |
| TTFT     | 客户端发出请求至首个有效生成 token，不包含人为等待或空流式事件                |
| 首次／后续轮   | 按上下文续接链和 reset 信息划分，分别给出样本量及 P50/P95              |
| 有效性      | 计划数、成功数、失败／超时数、P/D 抢占情况                           |
| 辅助信息     | 请求实际输入输出长度、总回放时长及已有排队指标                           |

保留 trace 请求 ID、session、actor 和轮次，显式展示失败及缺失样本，不静默删除异常记录。无法可靠获得的指标标为不可得，并说明缺少哪一环证据。

命中变化使用“百分点”；TTFT 改善率为：
```
(TTFT_L - TTFT_H) / TTFT_L × 100%
```

存在抢占时，不将本地输入计数解释为全部物理重算量。

结果必须注明：\*\*这是每档单次闭环回放的工程观察，尚未评估运行间波动。\*\*固定 trace 和释放规则不保证实际请求交错完全相同。

## 8. 交付与完成标准

在既有工作目录下保存：

- 一份汇总报告及 `summary.csv`。
- 一张结果表：容量、命中、输入计算、首次／后续轮 TTFT、成功／失败／抢占和回放时长。
- 两张图：实际 P KV 容量—命中率；实际 P KV 容量—后续轮 TTFT P50/P95。
- 逐请求数据、启动及回放命令、配置、模型与运行时标识、metrics 快照和日志路径。

只画实际有效点，不补未运行的 M，不根据两三个点宣称找到精确饱和点。

报告必须回答：

> 容量是否改变？P 本地复用是否增加？输入计算是否减少？后续轮 TTFT 是否受益？证据支持到哪一步？

\*\*完成：\*\*取得有效 H/L 对照，完成上述交付，并作出符合证据强度的判断。正收益、第三次或第四次回放都不是完成条件。

\*\*部分完成或阻塞：\*\*资源不可用、核心路径不兼容、P 本地命中或缓存状态无法确认，或次数预算耗尽仍缺少有效对照。保留已有材料、失败命令和日志，明确未完成项；不能将部署阻塞解释为容量无效。

不开展 AFD、单体／P-D 对比、模型或 connector 横评、并发扫描、输出长度扫描、额外消融和常规重复实验。边界内常规操作与小修自主完成；需要突破四卡、固定负载、既定部署或四次预算时停止扩展并说明原因。

任务结束时按全局清理规则停止本轮服务、核验进程及 GPU 等资源残留、释放 SLURM 分配并确认终态，保留结果，不影响他人作业。