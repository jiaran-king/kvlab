> 2026-09-24 用户追加范围：HTML 暂缓，不作为本轮交付验收重点。优先可靠 CLI、清楚输入格式、结构化结果、可追溯请求和事件证据；保留既有页面，不新增 Agent 层。

# Codex Goal：KVLab 下一版——AFD/baseline 缓存命中与 local-compute 对照交付

日期：2026-09-24。  
起点：当前 native v2 工作区及其最新结果。  
性质：增量补齐与交付，不重新开始。“下一版”是任务名称，不要求升级已有文件 schema。

## 1. 本阶段目标与边界

在不运行真实模型的条件下，让用户**无需修改模拟核心，仅通过输入文件或 CLI**，就能替换受支持的多轮 workload，配置 AFD/baseline 场景的有效 KV 容量、P 缓存域数量、冻结路由、客户端并发、每 P `max_num_seqs` 和 MBT，重新运行并比较：

- Prefix-cache 的实际采用比例，以及单列的原生查询命中率。
- Local-compute，即累计实际逻辑完成的本地输入 token 处理量。

结果须能从全局下钻到每个 P、每个请求及相关缓存事件，解释差异或明确证据不足。

本轮不估算 TTFT、吞吐、SLO、FLOPs 或硬件利用率，不增加时间模型、执行耗时校准、动态 router、FFN/通信性能模型。**缺少这些能力，不是本阶段的缺陷、阻塞或验收失败。**

结论限定为：给定缓存机制、有效预算、负载、路由和逻辑推进协议下的缓存与输入处理结果。两个核心指标相同可以是有效结果，不要求 AFD 必然获益，不要求曲线单调。

## 2. 从实际 v2 继续，复用已完成工作

先阅读当前 `AGENTS.md`、Goal、`START_HERE`、实现及验收产物，以工作区实际版本为准。保留未提交修改、旧输入和历史结果，不将归档旧源码当成最新实现。

对话中 v2 已报告完成：

- 多 P 独立原生 Scheduler/缓存池与全局逻辑轮次。
- 冻结 routing，client/seqs/MBT 接入及定向原生验证。
- 1 P 回归、2 P 两预算运行。
- 跨场景比较、逐 P 页面和请求证据。

**先核对对应产物；已有直接证据的能力不重新实现。**

主要待核对事项包括：精确字节／实测块数预算的端到端输入、指标与比较口径、缺失的机制证据、干净目录复跑、交付包和最终验收。工作区已经完成的部分直接复用。

在同一份验收报告中简要区分“已有可复用”“本轮补齐”“目标资产缺口”。已修复的 workload 摘要绑定、incomplete 处理和来源标签问题保留回归，不重新开展全面审计。

技术路线保持：

**Python CPU 离线驱动＋锁定版本原生 Scheduler/缓存管理体系＋只读结果页。**

不得另写缓存算法或调度器，不建设第二个 JS 正式核心，不为 AFD 和 baseline 复制两套相同后端。

## 3. 精确预算输入与场景表达

### 3.1 预算必须真正作用于原生池

复用现有 `capacity`、`conditions`、`scenario` 等入口。每个运行点明确一种权威输入：

- 每 rank 的 KV 字节预算；或
- 目标原生配置的物理池块数。

保留原 GiB 扫描入口，但统一走同一条解析路径。

支持资源限制内的合法预算，不限定为几个整数 GiB 档位，不将可靠字节预算向上取整成整数 GiB。换算依据目标布局；只有锁定布局确实采用固定槽位 stride 时，才使用：

\[
N_{\text{blocks}}=\left\lfloor\frac{B_{\text{bytes}}}{\text{stride}}\right\rfloor
\]

明确物理池总块数与 null／保留块的关系，不重复扣除保留块。多组缓存遵守原生布局与协调器规则，不自行均分。

同时提供字节和块数时，按原生解析结果核对一致性。字节可以有不足一块的余量，不要求恰好等于 `块数×stride`。

**不同字节值映射到相同有效块数属于合法情况，报告为相同有效池容量。**

输出保留权威输入、解析块数、保留规则、布局标识与来源。只有块数证据时，不倒推出未经测量的“精确实机可用显存”。

预算必须贯穿原生实例、manifest、summary、比较、bundle 和页面，不能只增加元数据。使用稳定 run/scenario ID，不能仅以整数 GiB 作为结果身份；已有字段能表达就复用，不新建重复 schema。

### 3.2 区分实际目标场景与假设场景

优先检查已有授权资产中的启动日志、有效 KV 配置和显存账本，确认可用的 baseline/AFD 预算。来源分别标记 `measured`、`derived`、`assumed`，保留证据位置或推导输入。

场景需明确：

- 实际使用的缓存 profile/源码、模型、TP、KV dtype 和多组恢复规则。
- P 域数量、每域每 rank 预算。
- client/seqs/MBT、routing 和外部生命周期协议。

FFN 资源不自动提供 P 本地 KV；D 侧 KV 不计入本轮 P 前缀收益；TP rank 不是独立 P；多个 P 的容量不能合成共享逻辑池。不能将迁出的 FFN 权重全部直接计为新增 KV。

改变 seqs/MBT 后，旧实测预算是否仍适用需要核对；无法确认时，只按**给定 KV 预算的条件化实验**解释，不宣称实机可部署。

本轮不扩展任意模型/TP、逐 P 异构参数或新的 Ascend backend。实际目标机制与当前后端匹配时，可以使用其预算；不匹配时列出布局、必要状态或释放协议缺口，**不将 H20 profile 改名冒充 910C。**

真实资产缺失不阻塞已支持 profile 的工具交付：可以用明确标为 `assumed` 的 low/high 场景验收，但必须单列“真实目标 AFD/baseline 对照未验证”。

PPT 只作部署背景。其对照包含并行布局、路由和 MBT 差异，不从其中的 TTFT/SLO 数值反推 KV 预算、命中率或 local-compute，也不未经核对选定某组为默认部署。

## 4. 核验核心指标

沿用现有字段，统一 CLI、报告与页面口径。

| 指标 | 本轮定义 | 必须避免的问题 |
|---|---|---|
| 初次实际采用量 `H_initial` | 请求首次成功进入最终有效调度时，实际采用的输入前缀 token 数 | 失败候选不算采用；恢复时采用量不反复累加成初次采用量 |
| 实际前缀采用比例 | 完整运行的 `ΣH_initial / Σinput_tokens` | 抢占下不冒充净节省比例；不平均每请求或每 P 的百分比 |
| 原生查询命中率 | 按锁定版本的查询语义计算 `Σhits / Σqueries` | 与实际采用比例分开命名；不为统计额外调用查询 |
| `local-compute` | 最终确实逻辑完成的批次中，本地输入位置的累计处理次数 | 不计回滚候选、未执行批次、D 输出或内部结束握手 token |

零分母为 NA，不填成零命中或零收益。

Local-compute 的区间必须使用**该批实际采用／恢复后的输入起点**，与真实输入位置范围相交；逻辑执行完成后再入账。不能依赖抢占前的旧 `num_computed_tokens` 快照。抢占恢复后同一输入位置确实再次处理，必须再次计入。

需要报告**重复本地输入处理量**时，以已完成输入区间账本支持：

\[
C_{\text{repeat}}=C_{\text{total}}-U_{\text{compute}}
\]

其中，\(U_{\text{compute}}\) 是该请求至少本地处理过一次的输入位置并集长度。

这一指标表示**同一请求内的重复处理**，不是全系统全部重复前缀计算。公式本身不证明重复处理的原因；只有相关事件能够关联到抢占恢复时，才将对应部分称为“抢占重算量”。

复用已有区间记录，确有缺口才补最小记录，不建立大型逐 token 位图。旧结果缺少证据时标 NA，不用 `input−hit` 伪造分解。

无抢占、无外部 KV 且符合当前协议的案例，可以检查：

\[
I=H_{\text{initial}}+C_{\text{total}}
\]

但不能将其作为所有请求的通用等式。

完整运行的主收益为：

\[
\Delta C=C_{\text{baseline}}-C_{\text{variant}}
\]

相对减少比例为 \(\Delta C/C_{\text{baseline}}\)，允许负值；分母为零时为 NA。全局计数由各 P 求和，比例由分子分母求和后计算。

未完成运行保留部分结果和状态，但不混入完整负载的总体收益，也不悄悄改为“共同完成请求子集”比较。

## 5. 保留并发和冻结路由

继续支持 workload、capacity、`client_max_in_flight`、`p_max_num_seqs`、`p_max_num_batched_tokens`、`p_domains` 和冻结 routing。参数必须进入实际原生配置并如实记录，不静默修改非法组合、缩减并发或退回 all-p0。

保持同 P 多活跃请求跨步持有 KV、多 P 独立缓存池、全局客户端在途上限，以及既定的 P 交接、引用释放和 ClientComplete 边界。**不能因为只看命中和计算，就退回每 P 串行历史缓存模型。**

输入继续冻结完整内容身份、依赖、输出来源、随机种子和 request→P 落点。分别保留 `send_after` 与 `context_after` 语义，后继输入使用既定内容。内部 P 结束握手与 workload 原始输出分开，不将实际输出长度统一改成 1。

Routing 优先使用已有真实逐请求落点，其次可验证重建，最后是明确的确定性规则，并标注来源。不从每 P 总请求数恢复逐请求映射，不把 proxy 地址当成后端缓存域。缺项、重复项、错误域和错误 workload 摘要应在原生运行前拒绝。

每个容量点重新产生准入、命中、批次、完成与释放，不回填参考运行的结果。冻结落点不等于冻结真实时序；维持现有逻辑步协议，不将毫秒或 RPS 自动换成逻辑步。

活跃引用、可回收缓存、等待、抢占、恢复与移除作为机制证据：

- 物理块按保留块、活跃引用块、无引用但有有效缓存身份的块、无引用且无有效身份的块互斥计数，共享块只计一次。
- Free queue 中可以包含仍有有效前缀的块。
- 全局峰值在同一采样边界求和后取峰，不相加各 P 不同时刻的峰值。
- 只有证据足够时才将等待归因为容量；序列名额、MBT、依赖等限制需区分，无法确认则为 `unknown`。
- 引用释放不等于驱逐，preempt 不等于所有前缀丢失，Removed 条目数不等于物理块数。
- 观测不得改变原生决策或额外调用有副作用的查询／分配。

**只解释选中比较对的最早关键差异及必要前驱，不建设自动全因果分类系统。**

主指标相同可以报告“本条件下未观察到命中／输入处理收益；等待或占用可以不同”，不扩大成“没有任何性能收益”。

## 6. 比较口径与必要验证

### 6.1 比较方式

复用已有三种比较：

| 方式 | 要求 |
|---|---|
| `capacity-only` | 固定 workload、缓存机制、P 数、routing、调度与外部协议，仅预算允许不同 |
| `single-variable` | 一次改变一项已声明变量 |
| `deployment-conditional` | 显式列出拓扑、预算、调度、routing 的联合差异，不将全部差异归因为显存释放 |

**`capacity-only` 允许有效容量相同的等价性对照。**标注“有效容量未变化”，不拒绝比较，也不将其解释为扩容收益。

可比性基于有效语义判断。Run ID、输出路径、展示标签和等价预算表达不应被误判为实验变量；实际源码、输入内容和未声明的机制差异不能靠场景名称掩盖。

一个 incomplete 点不阻止其他完整可比点比较。证据绑定正确的 run、比较对、P、request ID 和输入摘要，不能用另一份同 ID、同长度但不同 token 的 workload 解释本次运行。

### 6.2 预算与计数小回归

小测试覆盖：

- 非整数 GiB 对应的精确字节。
- 块数权威输入。
- 等价字节／块数及冲突输入。
- 不足原生最低要求的预算。
- 同容量不同场景的结果身份。
- 不同字节映射到相同有效块数时的比较行为。

至少一次小型原生运行证明池实际采用解析后的预算，不只是验证 JSON。

默认 1 P、多 P 隔离、三个参数和抢占恢复等已有证据优先复用。纯后处理或文档修改不触发完整原生重跑；影响原生预算、计数或推进语义时，补对应小型原生回归。正确性修复可以改变错误旧数字，但须解释原因。

### 6.3 两类竞争的直接证据

| 类型 | 要验证什么 | 如何控制规模 |
|---|---|---|
| 历史前缀竞争 | 前缀建立、释放后仍可复用，相关状态因容量竞争变化，回访采用与 local-compute 有可解释差异 | 优先复用同 profile 既有容量敏感证据；缺少时增加一组短 low/high 对照 |
| 活跃容量竞争 | 多请求实际持有 KV，小预算出现有直接依据的容量阻塞或原生支持的抢占，随后能够推进 | 每个请求单独能放下；client/seqs/MBT 足以排除明显其他限制；无可复用前缀的案例允许两个主指标相同 |

**已有同等证据充分时不新增案例。**

确有缺口时，新增机制负载每类原则上 3–8 个请求、两个预算点。先按目标多组布局和完整输入准入要求估算可行区。

为寻找容量受限条件，初始方案后最多进行一次有依据的 fixture／预算调整；仍未覆盖目标路径时，报告定位结果与剩余缺口，不盲扫。**发现明确实现错误时，应修复并运行受影响回归，此类修复不计入实验参数调整次数。**

断言按原生对齐、尾部和必要状态条件编写，不能假定整个输入都能命中。维持 full-sequence admission 等目标规则，不改策略来制造抢占。没有走到容量受限路径，不能仅凭 `running > 1` 宣布验证通过。

### 6.4 一次可复跑的使用闭环

给出可直接修改的场景配置和真实可执行命令：

**导入／导出 workload → 校验 → 设置两个预算 → 原生 CPU 运行 → 比较 → 证据 → bundle → 只读页面。**

从干净解压目录，用最终代码和精确预算入口实际跑通一对小场景；该对可以与机制回归复用，不额外要求一轮 76／425 全量。

以不同的合法输入或小型 Replay 导出示例确认“不改核心即可换 workload”。导出依赖与原生运行依赖分开说明，不重复搭建庞大环境。

已有 76 请求两档相同的结果继续保留，不为了出现正收益持续修改它。从 trace 选择子集时保留依赖闭包和来源，不以重复请求 ID 或仅修改 ID 制造“新内容”。人工重建内容标明 synthetic，不称 captured tokens。

本轮不做容量×并发×MBT×routing 排列扫描，不启动真实 GPU/NPU 模型实验。

## 7. 交付与验收

交付一个可复跑包，包含必要源码／补丁和依赖说明、样例输入、预算和路由配置、原生结果及只读查看入口。包内提供一份 `START_HERE` 和一份验收报告，不重复生成多种报告格式或打包无关环境。

`START_HERE` 必须给出最终代码实际执行过的操作：

- 换 workload。
- 输入精确字节或块数。
- 改 client/seqs/MBT。
- 选择多 P 与冻结 routing。
- 运行容量或部署条件对照。
- 查看主指标和请求证据。
- 处理输入错误与 incomplete。

不能写不存在的模块、过期条件文件或仅作者机器可访问的路径。配置在 CLI／文件中完成，页面只读，不建设 Web 驱动服务。浏览器受环境策略限制时如实列出未验证项，不绕过策略，也不把 DOM 测试称为真实浏览器验收。

最终验收回答六个问题：

1. 用户能否不改核心，更换受支持输入与条件，产生新结果？
2. 精确字节／块数是否作用于原生池，预算口径是否清楚？
3. 实际采用、查询命中和 local-compute 是否能逐请求核对，含适用的抢占路径？
4. 同 P 并发、多 P 隔离、冻结路由与两类竞争是否有直接证据？
5. 比较是否使用完整可比结果，证据是否绑定正确请求和前置事件？
6. 工具流程验证到哪里，实际 AFD/baseline 的预算、缓存 profile 和 routing 各是什么真实性级别？

**“工具流程通过”与“真实目标 AFD/baseline 缓存对照通过”分别标注。**后者只有预算、目标机制和场景信息足以支持时才成立，否则具体列明缺口。

没有时间模型不进入未完成清单；主指标无差异不影响正确工具流程的验收。未覆盖的必需机制证据应如实列为未完成，不能以其他测试通过替代。

## 8. 执行顺序与停止条件

依次自主推进：

**核对最新工作区并复用证据 → 精确预算贯穿 → 指标与比较核验 → 只补缺失的短机制证据 → 干净目录复跑 → 打包验收。**

复用现有模块，保持修改面小。不为未来性能预测预建插件层、事件总线、自动调参平台或第二套后端，不堆叠与本轮目标无关的防御框架。

沿用当前授权的 CPU/SLURM、服务器健康和资源上限规则，不在登录节点长时间计算，不修改其他作业或线上服务。D/Z 项沿用已确认的最新日志观察规则，不重新引入已撤销的单项拒绝；其他检查不擅自移除，不连续换节点重投。

不下载权重、不启动真实模型、不升级引擎、不发 PR/push、不删除历史证据。预算、profile 或环境缺失时保留已完成工作和最小阻塞，不靠改标签、静默缩负载、降低并发或伪造来源通过验收。

**满足工具验收，并明确真实场景状态后结束本阶段。不要追求漂亮曲线，不拟合 PPT 的 TTFT，不将时间／吞吐预测重新加入任务，也不要再次要求用户选择已经确定的路线。**