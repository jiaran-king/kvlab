# Codex Goal：KVLab 可复用缓存实验分析工具

## 一、目标

在现有原生 vLLM 离线模拟器基础上，交付一版可替换负载、可配置实验条件、可追溯解释、可独立复跑的 KV Cache 分析工具。

用户提供一份受支持的请求负载、一个目标部署配置和一组实验条件，无需修改模拟核心，即可分析不同容量下的前缀采用、累计输入处理、历史缓存竞争、同 P 活跃请求竞争、等待及缓存移除；从总体差异追到具体请求与相关状态，并据此选择值得开展的实机实验。

本轮直接完成实现和使用验收。验收以用户能完成的实验分析为中心，不以测试数量、跑完某份固定负载或生成压缩包作为完成标准。

## 二、继承的基础与范围

保留 Python 离线驱动＋锁定版本的原生 Scheduler／缓存管理体系＋KVLab 只读结果页。复用已经完成的 H20 五组缓存、同 P 并发、释放回收、受控容量扫描及定向抢占恢复证据；已有修复且有对应验证的项目不重新全面审计。

H20 是首个已接通的目标部署，425 请求是参考与回归案例，12／24／48 GiB 是历史实验点。它们不构成通用流程的永久约束。当前阶段仍以一个逻辑 P、目标 H20 模型和缓存布局为运行范围；同一 P 内多个活跃请求跨步持有和申请 KV 是必要能力。

本轮不新增 910C 适配、多 P 动态路由、其他模型、AFD、详细性能模型或通用插件平台。明确目标部署边界，使未来接入 Ascend 910C 时能够复用负载、分析和展示流程；不能把“以后支持”写成已经验证的跨平台能力。

不运行真实模型，不启动新的 GPU/NPU 回放，不建设本地服务，不继续发展第二套正式 JavaScript 缓存核心。

## 三、分清目标部署、实验条件与通用流程

优先整理现有 layout、conditions 和结果格式，不为此建设复杂框架。

目标部署集中描述模型、引擎版本与必要补丁、TP、KV 精度、缓存组与块布局、恢复规则，以及当前实现支持的调度和生命周期能力。需要锁定的规则有明确来源，不在运行器、比较器和页面中重复散布 H20 特例。

实验条件描述本次容量列表、受支持的调度参数、客户端约束、初始状态和逻辑释放协议。逐项说明哪些可调整、哪些由目标部署固定、哪些不支持。配置字段必须实际生效或明确拒绝，不能接受后忽略，也不能仅用整个配置文件核验用户是否填写了历史实验原值。

通用运行、比较、打包和展示从有效输入与运行产物读取信息，不自行假设三档容量、425 个请求、固定请求 ID 或某个历史目录。工作区绝对路径可以作为来源记录，不能成为独立后处理的必需依赖。

## 四、容量真正作为输入

容量列表由 conditions 或现有 CLI 入口提供，并贯穿校验、原生运行、对照、证据生成、结果包和页面。支持目标布局与合理资源范围内的用户容量点，不再维护 12／24／48 的白名单，也不通过增加 16／32 等特例完成任务。

依据目标布局及对应运行时的预算规则，将每卡 KV 字节预算换算为实际物理块数；记录预算、有效字节、块数、保留量和取整规则。不得把设备总显存当作 KV 预算，或将 TP 多卡预算相加扩大逻辑缓存容量。

现有 H20 12／24／48 GiB 的 12,393／24,786／49,572 块是必须核对的回归点。若源码证据表明既有映射存在错误，先说明依据并修复，不能为了保持旧数字而保留错误。无效输入与资源超限明确报错；运行中无法推进与输入格式不合法分别报告。

## 五、替换负载的完整流程

通过现有 AgentInfer／Replay exporter，修改一份受支持的 Replay 配置，生成不同于现有 425 请求的小型冻结负载。选择完整会话或使用上游明确支持的轮次配置；不通过重命名旧文件、随意截断导出 JSON、手写长度或人工增加共享前缀代替替换负载。

保留完整 token 内容、请求身份、会话与上下文关系、发送依赖、输出来源和稳定顺序。修通 exporter 与原生 loader 的来源标记、execution profile 及其他受支持字段。对无法表达的节点或执行语义明确拒绝，不静默丢弃。使用合成内容时保留真实性标记，不能冒称历史 capture；D 输出进入后继输入不代表它已存在于 P 缓存中。

先冻结负载，再扫描容量。输入内容、依赖、外部规则和调度条件固定，内部准入、批组成、完成与回收顺序由各容量的原生 Scheduler 自行产生。

这份新负载运行两个容量点，其中至少一个不是原三档，例如 16／24 GiB。两个容量结果相同也可通过使用验收；不要为了制造收益反复修改负载。两点验证是检查工具可复用性的案例，不是把产品能力重新限定为这两个点。

## 六、正确性和失败处理

原生核心继续负责调度选择、准入、前缀查找、多组共同恢复、分配、引用和回收。离线驱动提供有明确语义的处理完成、传输完成与客户端完成通知。只统计真正完成的逻辑输入处理，不能把分配尝试或取消的批次计为计算。

明确区分 P prefill 完成、传输完成、P 引用释放和客户端完成；暂未获得调度份额不代表释放 KV。保持有限的步数、墙钟和日志上限，区分合法等待、未来释放、配置无法执行与达到运行上限；达到上限不等于证明该容量永远不可执行。

每个请求的初次缓存采用量、累计输入处理、等待、抢占、恢复及完成状态可追溯。累计处理包含适用的重算，不能强制等于输入减初次命中；恢复采用不能重复累计到一次输入缓存比例。

所有计划容量点均须出现在总览。独立容量点失败时保存已有产物，并在环境健康且可以独立继续时处理剩余点；若必须终止整次扫描，未运行点也要明确标记及说明原因，不能消失。整体输入无效则在计算前拒绝。失败、未运行和完成不得混为成功结果。

核对摘要与逐请求计数、实际生效配置与声明配置的一致性。已有覆盖这些问题的修复直接复用；只对改动和具体缺口补针对性验证。

## 七、把证据组织成可使用的分析

保留查询命中率、实际输入缓存占比、累计输入处理、按组移除 hash 条目以及活跃占用和生命周期信息。历史 local_compute 按原定义单列；不能用查询未命中替代，不能换算为未经校准的 GPU 时间。引用释放、抢占和缓存移除分别表达，不把所有 BlockRemoved 都认定为纯容量驱逐。

按稳定请求身份对齐容量结果，同时保留各自的实际执行顺序。选择一个证据较充分的容量差异，优先复用现有案例，组织以下信息：

- 哪个请求、哪段前缀存在复用机会，各容量实际采用与处理多少。
- 相关内容此前建立或采用的证据，以及中间相关的移除、释放和竞争记录。
- 请求访问时可取得的活跃引用、空闲块和必要恢复状态信息。
- 哪些事实足以支持解释，哪些只能说明相关性，哪些因事件窗口或历史记录缺失而未知。

页面提供可读的容量比较、请求详情和关键事件顺序，原始 JSON 作为补充。用户应能从总览进入首次差异请求，再查看两边有效条件与证据。无需为每次 miss 给出唯一根因，也无需补建全量逐块录像；缺失的证据不可由推测填充。

历史参考按来源和可比性明确展示。新负载不能自动套用 C12/F12/F24 的同内容结论；非历史容量不虚构对应实测值。H20 历史差异继续保留，当前逻辑协议的分析能力与实机预测可信度分别报告。

## 八、验收与交付

完成以下一次完整使用流程：

新 Replay 配置 → 导出 workload → 校验 workload 与 conditions → 两容量原生 CPU 运行 → 独立比较和证据生成 → 结果打包 → KVLab 导入、容量切换和请求证据查看。

在干净解压目录实际验证后处理和页面构建；文档列明原生运行环境、依赖、目标配置及命令，外部必需资产有明确获取或定位方式，不依赖 Codex 临时补脚本或记忆旧目录。明确哪些步骤可在普通本地环境执行，哪些需要已配置的目标原生 CPU 环境。

页面覆盖成功结果以及已有的 incomplete／失败结果。使用获准且可用的真实浏览器环境检查导入、切换、详情、配置和边界说明。若浏览器访问被工具策略明确禁止，遵守限制，保留 DOM 测试证据并标记真实交互未验收，不反复绕路尝试或把 DOM 测试冒充浏览器验收。

只安排一份小型替换负载的两点新实验，外加具体正确性修复所需的定向复核。只有修改影响模拟语义时才运行相应的原生回归；展示和后处理修改不自动触发整套 425 请求重跑。已有抢占恢复不再重复盲扫验证，不靠调参拟合历史命中率。

最终交付：

1. 可运行源码、明确的目标部署入口和受支持条件说明。
2. 新 Replay 配置、冻结 workload、conditions 与两点运行产物。
3. 可独立生成的对照、解释证据、结果包和 KVLab 页面。
4. 一份 START_HERE 使用入口，包含换负载、改容量、运行、查看和失败处理命令。
5. 一份简洁验收报告，区分工具可用性、机制验证范围、历史可比性和未验证项。
6. 一个内容自洽、可复跑的交付压缩包。

## 九、完成与操作边界

当用户能够不改源码、不依赖临时人工脚本，替换受支持负载与容量，完成运行并理解结果及失败原因，且所有承诺的能力有对应证据时，本阶段可交付。

仅真实浏览器等外部环境验收受阻、核心流程已完成且没有已知阻断正确性的缺陷时，可以有限交付，并明确未验收范围。输入语义错误、条件静默失效、计数错误、结果状态错误或主要使用流程无法完成属于阻塞，不能以有限交付豁免。有限交付不等于所有验收均通过。

交付报告和产物后结束本阶段，不自行扩展新平台或实验矩阵。遵守既有服务器健康与 H20 SLURM 指南；计算仅在合法分配内进行，预检拒绝时不连续换节点重投，不绕过健康限制。保留历史证据，不覆盖用户修改，不 push、不发布 PR。
