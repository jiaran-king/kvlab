# Codex Goal：交付可替换负载、可选择容量的 KVLab v1

## 目标与完成定义

在现有 Python 原生 vLLM Scheduler／五组缓存离线模拟器基础上，交付一个可以实际复用的 KVLab v1：用户用**受支持的 Replay 配置**导出另一份冻结负载，在 conditions 中选择 P 每卡 KV 容量，无需修改模拟核心，运行离线容量对照，并在 KVLab 查看有效条件、逐请求缓存采用、累计输入处理、同 P 活跃占用及相关事件，追溯至少一个有证据支持的容量差异。

H20 的 425 请求三档扫描继续作为已完成的首个受控案例及历史对照；本轮重点是验证工具链可复用，不通过拟合历史数字、盲扫压力参数或反复打包来代替使用验收。保留原生调度、缓存组共同恢复、同 P 活跃并发、独立的抢占恢复机制案例。**不运行模型，不新增 GPU/NPU 回放，不建设本地服务，不引入第二套正式模拟核心。**

完成条件是：一份确实不同的 Replay 负载经导出→校验→两个自选容量的原生 CPU 运行→独立对照/结果包→KVLab 查看，形成可复跑产物；原 12/24/48 GiB 档的块数及受影响行为保持一致；失败点不被隐藏或标为成功；至少一个已有或新产生的容量差异能沿请求、状态和事件解释到证据边界；最终提供使用入口、验收报告和局限说明。

## 1. 从现有状态出发

先核对当前工作区和最新审查包，以实际文件与运行记录为准。原生 425 请求扫描已在 12/24/48 GiB 完成，H20 历史对照尚非精确复现。作业 3826 已在**单独的 1,800 块、2,048-token 分块变体**验证 B 的抢占、恢复、重算和最终释放；不能因此声称历史 425 请求发生过抢占。本轮不再重复该机制实验，除非后续修改直接影响这条路径。

当前代码中的具体交付缺口需要正面处理：`run.py` 的三档 `BLOCKS` 表与严格 `[12,24,48]` 校验、`compare.py`／`bundle.py`／原生结果页面的固定三档遍历；Replay exporter 与原生 loader 对 `captured_tokens`、默认 execution profile 等输入语义并非自然对接；一次非预期容量失败可能使后续容量点不再运行。先验证每一处的实际影响，再以最小改动修复。不要把旧浏览器 JavaScript 模拟核心当作原生扫描的替代入口。

## 2. 输入和容量

沿用一个逻辑 P、目标 H20 TP2 五组布局、锁定的原生 vLLM 版本和已验证的 P 调度配置。`workload＋conditions` 是公开使用入口；不支持的模型、布局、路由、执行协议和字段明确报错，不静默回退。

容量列表从 conditions 读取，允许在**目标 H20 布局和已明确的资源范围内选择整数 GiB 点**，不再把 12/24/48 写成唯一合法列表。物理块数从历史布局的每块字节步长和每卡字节预算推导，说明取整规则及它与目标运行时预算的关系；不要简单按两张 TP 卡相加。验收必须覆盖一个非旧三档容量（建议 16 GiB）以及另一个不同容量，并确认 12/24/48 仍精确得到 12,393／24,786／49,572 块。无效、重复、过大或不可执行的容量应在计算前报清楚原因。manifest 记录用户预算、换算出的块数、布局来源和实际生效的原生配置。

导出一份**不同于内置 425 请求**的小型负载：从已有 AgentInfer Trace 中更换受支持的 Replay 配置，选取完整会话或受支持的轮次设置，用现有 exporter 生成；不得仅重命名旧文件、截断已导出 JSON，或手写长度代替 token 内容。保留完整 token 序列、请求身份、依赖和来源标记。使用固定 tokenizer 生成的内容应标为 trace 驱动合成重建；使用运行时 capture 则保留 capture 的真实性与来源。修通 exporter→native loader 的受支持字段和 profile，不将不支持的 timing 节点或上下文语义悄悄丢弃。D 输出可以成为后继输入，但不自动成为 P 缓存。

同一份冻结负载在两个容量点保持内容、依赖、逻辑释放协议、调度参数和初始状态一致，只改变容量。两个点可以得出相同结果；不制造正收益作为通过条件。

## 3. 运行、失败和结果解释

原生 Scheduler 决定等待、批次、准入、前缀采用、抢占及缓存回收。离线驱动只提供明确的逻辑处理完成、传输完成与引用释放通知；不能写入第二套策略。保持有限步数和日志上限。每个指定容量点均须在总览中出现：正常完成有完整请求结果；中途无法完成有 `incomplete`、原因、已执行的步与事件、全部计划请求行及明确的指标覆盖范围。一次普通点失败不能让其他点无记录；输入整体无效则在运行前失败。不要把异常吞掉后继续标为成功。

动态容量对照按冻结负载的稳定请求顺序对齐，保留各点实际调度步，不把同号步视为等价状态。比较器、结果包和页面均从实际容量列表生成，不假设固定三档；历史参考只在容量与负载来源适用时单独展示，不把新负载的 16 GiB 结果与 H20 的 12/24/48 GiB 历史运行硬配对。统计并清楚区分：运行时查询命中率、实际输入缓存占比、累计输入处理量（含适用的重算）、历史 `local_compute` 来源计数、各组 `BlockRemoved.block_hashes` 条目、等待／抢占／恢复／释放。缺少同口径数据时不相减或换算为 GPU 时间；不能把所有移除称为纯容量驱逐。

选择一个有意义的首次采用或处理差异，给出可检查的链条：请求与前缀身份、两容量各自采用和处理量、当时活跃引用与空闲块、附近相关建立／移除／释放事件及事件覆盖范围、能够支持的解释与仍未知的原因。可复用现有 425 请求第 38 条差异；不要求为每个 miss 自动给出唯一根因。页面能从总览跳到该请求、对应容量和证据，显示有效配置、内容真实性、逻辑协议以及 `incomplete` 状态。

## 4. 有界验收

按下面顺序执行，已有证据够用时不重跑：

1. 本地验证 exporter 与输入校验：新 Replay 配置产生不同的完整请求集合，依赖合法，来源与 token 数一致；不支持的组合明确拒绝。
2. 在遵守 H20 指南与 SLURM 健康门槛的前提下，运行**一份新负载、两个容量点**的 CPU 离线扫描；CPU 资源、时间和重试有上限。节点预检拒绝时不绕过、不连续换节点重投，保留阻塞记录。
3. 验证两点 manifest、summary、完整请求行、步、事件、独立 compare 与 bundle；检查非旧档容量的块换算和实际生效配置。对受影响的原三档路径做针对性回归；仅在模拟语义受改动影响时重跑 425 请求三档，不为展示或文档修改重跑。
4. 从干净解压目录按文档执行校验、对照、打包和页面构建。用允许的真实浏览器环境时，导入成功与 incomplete 结果，切换容量、定位请求、查看配置和证据。若可用浏览器仍明确禁止本地页面访问，遵守该限制，运行 DOM 控制器测试并将真实交互标为**未验收**；不得把 DOM 测试或绕路访问写成浏览器验收。

只做这一次小型替换负载的两点正式验证和由发现的正确性缺陷直接要求的定向复核。原生抢占机制已有正例，不继续盲扫块池；历史 12 GiB 差异保留为真实性边界，不靠调参拟合。发现输入语义、计数守恒、容量换算或结果状态错误时，先修复并验证，不能用“有限交付”掩盖。

## 5. 交付与停止条件

交付一个可复跑目录／压缩包，包含 CLI 使用入口、示例 Replay 配置及新 workload、conditions、两容量的原生输出、动态对照与可导入结果、关键差异证据、必要的验证日志、一份简洁验收报告。报告分别回答：如何换负载、如何改容量、哪些机制已经验证、这次新负载得到什么、H20 历史能对照到什么程度、页面交互与运行环境还有什么限制。

结论只分三类：

- **可交付**：替换负载和容量的端到端流程及页面交互均通过，没有已知阻断正确性的缺陷。
- **有限交付**：核心流程与计数可信，真实浏览器或外部环境导致某项无法验收；明确标记未验证范围和复跑入口。
- **阻塞**：无法可靠替换输入、容量条件静默失效、计数或状态错误、失败被标成成功等核心问题尚未解决；列出具体证据与所需条件，不宣称完成。

达到相应结论并完成交付后停止本阶段，不自行扩大为多模型、多 P 动态路由、AFD、Ascend、GPU 性能预测、全量历史时序重建或无期限实验。保留原始证据，不覆盖旧结果，不 push、不发布 PR；远端 CPU 工作必须按用户现有服务器健康与 H20 SLURM 指南执行。
