# Codex Goal：KVLab v2——多 P 冻结路由与 AFD KV 收益的条件化分析

日期：2026-09-23。
性质：下一阶段实施与验收目标，不表示相关扩展、真实 AFD 对照或 Ascend 适配已经完成。

## 1. 本阶段要交付什么

在现有 KVLab native v1 上，开放 workload、KV capacity、client concurrency、P max_num_seqs、P max_num_batched_tokens（MBT），增加多个独立 P 缓存域与冻结 request→P 路由输入。用户无需修改模拟核心，就能完成可复跑的参数对照，并从全局结果下钻到每个 P、每个请求及相关缓存事件。

最终服务于：分析 AFD 相对 non-AFD 释放的 Attention 侧显存，在转化为有效 KV 容量后，是否增加前缀复用、减少实际输入处理，以及这些收益受负载、并发和缓存域分布怎样影响。

本阶段首先建立“已验证缓存机制下，给定容量、拓扑和外部路由的条件化分析”能力。不实现 AFD 模型执行，不预测 TTFT/吞吐，不把两档手填容量自动称为真实 AFD 收益。

本轮的**工具验收**与**真实部署结论**分开：即使只能使用明确标记的假设预算和生成路由，也应完成可复跑工具；但只有目标缓存机制、AFD 与 non-AFD 的有效 KV 预算、实际拓扑及路由/时序条件均有足以支持相应结论的证据时，才能升级为真实部署分析。缺少这些材料时，最终结果只能回答“在所给固定条件下会怎样”，不能回答“真实 AFD 一定带来多少收益”。

技术路线保持：Python CPU 离线驱动＋锁定版本原生 vLLM Scheduler/缓存管理器/组协调器/BlockPool＋KVLab 只读结果页。原生核心继续决定准入、调度、采用、分配、回收、抢占和恢复。不得另写一套缓存算法或 JS 正式模拟核心。

## 2. 从当前实现继续，不重新开始

起点为实际工作区最新代码；核对 `KVLab_native_v1_20260923.zip` 与之后已有修复。若能取得 `KVLab_native_v1_review_20260923.md`，一并核对其具体问题；该外部审查文件缺失时记录来源缺口，不阻塞依据源码和产物开展工作。若工作区已有更新，以可核验的新版本为准，保留未提交修改与历史结果。

已有 76 请求、16/24 GiB 结果和旧 425 请求对照继续保留。新 76 请求是 `trace_driven_synthetic`，两档没有采用差异；它证明替换输入/容量的流程，不证明真实容量充分性。已有同 P 并发及定向抢占恢复证据按对应源码、配置与日志复用；缺少某份记录只标明该证据缺口，不盲目重扫。

先收掉尚未解决的 v1 后处理问题：

- 证据生成必须核对运行所用 workload 摘要，不能用同 ID、同长度的另一份 token 输入生成解释。
- 有 incomplete 点时仍校验各点输入与配置；保留失败状态，同时比较其他通过可比性检查的完整点。
- 修正 captured_tokens 来源标签、按比较对选择请求证据，以及实际复跑条件与 README 路径。

已修且有回归证据的项目不再重复审计。上述纯后处理修改不触发原生全量重跑；本轮调度与多 P 改动则必须有针对性的原生 CPU 回归。

## 3. 开放三个调度/并发参数

延续现有字段，不新建一套重复参数名称：

| 字段 | 本轮语义 | 原默认值 |
|---|---|---:|
| client_max_in_flight | 全局客户端在途上限；包括已提交后在 P 排队、执行、交接及尚未 ClientComplete 的请求 | 3 |
| p_max_num_seqs | 每个 P 的原生 Scheduler 活跃序列上限，不是所有 P 合计值 | 2 |
| p_max_num_batched_tokens | 每个 P 每次 schedule 的原生 token 预算，不是全系统共享预算 | 8192 |

本轮各 P 使用同一组调度参数，暂不扩展逐 P 异构调参。默认条件必须保留旧语义。支持合法的正整数与组合；以 client=1/3/8、seqs=1/2/4、MBT=4096/8192/16384 作为默认附近的验证候选，不跑全排列，也不把这些值写成永久白名单。

修改须贯穿 conditions 校验、原生配置构造、实际实例读取、manifest、比较器及页面。不能只删除固定值校验：当前 `make_vllm_config` 和 effective-profile 核验也包含默认值。可调项与本次 conditions 比较；固定缓存规则仍与目标 layout/源码比较。

核对锁定版本的 chunked-prefill、对齐、准入和配置约束；不合法组合启动前明确拒绝，不擅自钳制、向上取整或改回默认值。不能因未观察到并发就自行均分 MBT，也不能因容量压力而减少声明并发。

改变 seqs/MBT 后检查缓存布局和状态分配的适用条件。原生控制面能够离线运行，不证明实机对应 workspace/通信缓冲也能装下。固定 KV 预算的参数敏感性实验与固定整卡显存预算的可部署性判断必须分开；后者需要匹配这些参数的显存账本。

本轮仍固定模型、TP、KV dtype、多组布局、hash/恢复粒度、retention 和 full-sequence admission。非本轮支持的值继续明确拒绝，不放开任意 target profile。

## 4. 多 P：独立缓存域，共享明确的外部驱动

`p_domains` 表示逻辑 P 缓存域数量，不是 TP rank 数、设备数或笼统的 DP/EP 数。实现可接受资源限额内的正整数，重点验收 1 P 回归与 2 P 行为，不为 1/2/4 分别写特殊执行分支。

每个 P 独立持有 Scheduler、KVCacheManager/协调器/BlockPool、connector pending 状态和统计。共享不可变 token 与模型布局描述可以，共享 KV 池、可变引用计数或缓存身份状态不可以。物理块 ID 必须带 P 命名空间；相同前缀 hash 在两个 P 上不表示存在可跨域复用的 KV。

使用一个确定性的全局逻辑轮次驱动，不将每个 P 的完整 workload 串行跑完再拼结果。每轮按明确阶段推进：处理本轮到期的交接/引用释放和客户端完成；从全局就绪请求按稳定优先序占用客户端名额并提交到指定 P；各 P 至多推进一次原生 schedule/逻辑完成；登记后续事件与结果。保留目标原生核内部的有序可见性规则。

不同 P 的遍历顺序不能让某个 P 提前看到其他 P 本轮完成而获得额外提交机会。全局阶段与稳定平局规则写入 manifest；在 1 P 默认配置下维持原语义。多 P 一轮各推进一次仅为受控协议，不意味着不同 P 或 AFD/non-AFD 每批真实耗时相同，也不模拟跨 P 的 EP/FFN 集合同步。

同一请求时序和冻结路由在不同容量下使缓存效应更易比较，但容量或 AFD 改变实际处理速度时，真实排队、后续到达时的后端状态与动态路由可能随之改变。因此，多 P 逻辑轮次输出必须标为**固定外部路由与逻辑推进协议下的条件化结果**；不能据此推断真实负载均衡、排队时间或整体系统吞吐。

客户端名额只在 ClientComplete 释放。PReferenceRelease 不等于 ClientComplete，也不等于缓存内容被驱逐。保留现有正整数逻辑交接延迟及客户端完成规则，明确延迟以全局逻辑轮次计；不顺手新增 D/网络性能模型。

一个 P 等待资源时其他 P 仍可推进；未来交接/完成事件不能被误判为死锁。不因目标 P 繁忙而重新选 P。按总池规模做 CPU 内存、步数及事件上限预检，异常保留已取得的状态。不能把达到运行上限写成该容量永远不可运行。

## 5. 冻结 routing：输入路由结果，不实现闭环 router

优先使用独立的轻量 routing trace/映射文件引用同一 workload，不复制大 token 文件来制作路由变体。保留旧 all-p0 入口作为无歧义的兼容默认；显式提供 trace 后不得再静默退回 all-p0。

最小记录包括 request_id、assigned_p 和稳定的原始顺序；元信息包括绑定的 workload 摘要、缓存域标识映射、路由来源、生成方法/参考配置。有真实时间戳时保留时间定义及来源，但本轮不据此计算真实执行时间。

来源优先使用已有真实逐请求落点记录；否则从可验证的既有证据重建；再否则以明确的静态规则生成并冻结。至少交付一种确定性亲和映射和一个手工可解释的显式映射 fixture，不建设路由算法集合。生成结果须标记 actual_capture / reconstructed / policy_generated 等真实来源，不因它来自 Replay 就称为实测。

注意以下边界：

- 必须是 request→实际逻辑 P 缓存域。客户端访问的 proxy/API 地址，不一定唯一对应后端 P；多个 TP rank 不拆成多个 P。只有 332/82/11/0 这类总计，不能恢复逐请求路由。
- 到达时间＋动态 router 算法不一定足以重建真实落点；还可能需要当时后端队列、完成与状态记录。缺失时不自行造一个耗时模型后称真实 routing。
- 核对请求覆盖、唯一性、合法落点、workload 身份与稳定顺序。缺项、重复项、额外不明请求或目标域不存在都报错，不取模、不自动补映射。
- 本轮不模拟重试、请求迁移或一次请求跨多个 P 的执行。捕获数据存在这些情况时明确拒绝该范围，不能只取最后一次 attempt 冒充完整轨迹。
- 本轮时间协议仍是 stable-ready-order-no-wall-time。trace 的顺序可用作已就绪请求的优先序；实际提交仍受声明依赖和全局在途上限约束。不能使用未来完成事件，也不能强制尚未就绪的后继请求进入。

每个容量点的 assigned_p 保持一致，但实际准入、批组成、完成和释放由该点重新产生。允许就绪/提交顺序随内部演化变化并记录；不能固定参考运行的命中、驱逐或原生批次来制造一致性。

必须在报告中区分：冻结 request→P，不等于冻结真实时序；真实来源的 routing，不等于本次完整实机回放。动态路由在参考容量下生成的 trace，应用于其他容量时属于固定外部路由的条件化比较，不代表该容量会自然产生同一路由。

## 6. AFD/non-AFD：部署说明、KV 预算和证据级别

复用同一个已验证原生缓存 profile 时，不为 AFD/non-AFD 复制两套缓存后端。增加或复用一个简洁的部署/场景描述，将“模拟哪个缓存机制”与“这次资源如何配置”分开。

场景至少能记录：scenario_id、target profile/源码/patch、P 缓存域数、P/Attention/FFN/D 的资源分配和 TP/DP/EP 含义、每域每 rank 有效 KV 预算/块数、调度参数、routing 引用，以及数值来源。资源信息未知可以明确留空，但不得据此声称等卡数或等成本公平比较。

本轮必做场景可以使用相同 target profile、各 P 同构的容量配置；不要求实现不同模型、逐 rank 异构布局或新的 Ascend backend。使用预算字节或实测有效块数作为单一权威输入，现有 GiB 列表保留为容量扫描入口；多个容量表示同时出现时必须核对一致性，不静默覆盖。不能把可靠预算向上取整成更大的 GiB。暂不支持的范围明确拒绝，不为了 AFD 标签套用错误布局。

显存账本优先复用既有实测/可验证推导，KVLab 不自动猜测 AFD 释放多少显存。对预算来源至少区分 measured、derived、assumed；派生记录其输入和假设。总可用额度减去不重叠的权重、运行时、通信及保留开销后，才是 KV 预算。不能把迁出的 FFN 权重全部直接记为新增 KV，也不能重复扣除同一开销。

比较还必须明确：FFN 卡算在总资源中但不自动提供 P 本地 KV；D 上的 KV 不计入本轮 P 前缀收益；TP 多 rank 字节和全体 P 字节可以用于物理资源账目，但不能相加成一个可共享的逻辑缓存池。AFD 与 non-AFD 的 Attention 卡数、P 缓存域数或布局不同，不能只比较每卡 GiB 后宣称整体收益。

真实 AFD 的缓存机制、布局、必要状态、P 引用释放协议与本轮 target 不一致时，列出适配缺口。不能仅将 profile 名改为 afd 就称其已被模拟。目标实际是 910C 而当前只验证 H20 时，只能交付相应预算材料和迁移缺口，不用 H20 的字节步长与缓存规则冒充 910C 结果。

**真实 AFD 收益结论的证据门槛：**至少能够追溯并核对两套部署的有效 P KV 预算及显存账本、实际 P 缓存域与 TP/DP/EP 映射、目标原生缓存机制/布局/释放规则，以及所用 workload 和路由/时序的来源与适用范围。若要把结果称为实机趋势或容量建议，还须用可比的目标系统基线核对关键缓存指标，并说明尚未捕获的动态路由和耗时反馈会怎样限制外推。当前没有这些证据时，保留 assumed/derived 场景和待验证清单，不把工具验收阻塞成无限实机调查，也不写出“AFD 已验证节省 X token”的结论。

## 7. 对照模式与输出

仍沿用现有 manifest、summary、requests、steps、events、comparison、evidence、bundle；只增加必需字段。使用稳定的 run/scenario ID 区分同容量不同参数的结果，不再把容量值当作唯一身份。兼容旧单 P 结果，不改写旧输入/运行数字。

比较器区分三类比较，并检查非实验变量：

| 类型 | 必须保持一致 | 允许变化 | 能解释什么 |
|---|---|---|---|
| capacity-only | workload 内容、target 缓存机制/布局、P 数、routing、调度/并发、外部生命周期规则、冷起点 | 每 P 有效 KV 预算 | 固定协议下容量变化的缓存效应 |
| 单变量敏感性 | 除声明变量外的上述条件 | 一次只变 client、seqs、MBT 或 routing 中一项 | 所选变量在该协议下的影响 |
| deployment-conditional | 同一请求内容/输出来源和明确的比较口径 | 显式声明的拓扑、预算、routing 等差异 | 给定部署与外部路由的联合变化，不单独归因为显存释放 |

本轮必须跑通 capacity-only，并完成新旋钮及 routing 的定向验证；deployment-conditional 的输入、标识和比较边界应可表达，不以缺少真实 AFD 资产阻塞其他工具能力。不要为此搭建通用实验平台。

当两套部署的 P 数或落点不同，分别记录各自路由与来源；此时总体差异同时包含容量、拓扑、分流和由逻辑推进协议产生的影响，不能拆成单一“AFD 释放显存”的因果收益。报告与页面对每个对照明确显示：比较类型、固定与变化的条件、预算和 routing 证据级别、目标 profile 是否已验证，以及是否具备真实部署结论的证据。证据缺口直接展示，不让 `scenario_id` 或 AFD 标签替代证明。

全局和每 P 展示请求数/输入量、初次采用、累计输入处理、原生查询命中、活跃引用与可回收缓存、等待/抢占/恢复及按组事件。全局比例使用分子分母求和，不能平均各 P 百分比；空 P 标记空/NA。全局占用峰值按同一逻辑轮次求和后取峰，不能相加各 P 不同时刻的峰值。

实际输入处理只计逻辑完成的输入位置，排除取消批次、非输入 token 和未执行候选；抢占后同一输入位置再次处理须计入累计量。需要报告“抢占重算量”时，以已完成输入区间账本支持；没有这种口径不得用 input−hit 等差值冒充。修改 MBT/seqs 后要核对这些边界，不强制沿用无抢占等式。

主收益写为：delta_processed = C_baseline − C_variant；相对减少比例在 C_baseline>0 时为 delta_processed/C_baseline。采用差异、重复处理及事件分别解释；这是逻辑输入 token 处理量，不是 FLOPs、毫秒或吞吐，也不自动等于所有跨请求重复 prefill。

对外解释分三层：假设预算/生成路由仅为工具演示；有来源预算与冻结路由支持固定条件下的缓存行为比较；真实 AFD 部署收益还取决于目标机制、有效预算、路由/时序及基线验证。结果页和报告不得省略这层解释，尤其不能把逻辑轮次的全局峰值、等待步数或 `delta_processed` 直接换算为实际负载均衡或性能收益。

请求详情绑定 run/比较对、P、request_id 和输入摘要。解释区分本 P 未建立过前缀、曾建立后的相关状态变化、迁移落点与内容差异等证据，不因“在另一 P 见过”就断言一定会命中；证据不足保留 unknown。Dropped/窗口外事件不能当作零事件。

## 8. 最小验证与实验预算

按以下顺序推进，不做所有参数的笛卡尔积扫描。

### A. 默认兼容与三个旋钮

先运行本地输入/后处理回归。原生 CPU 环境可用时，用已冻结小负载和一个旧容量点完成默认 1 P 回归，逐请求采用/处理与关键生命周期保持一致；新增元数据可以不同。若语义必须修正，给出定位与依据，不为保留旧数字掩盖 bug。

用少量小型机制例分别验证全局 client 上限、每 P running 上限、每步 MBT 预算和实际生效值。选择确实可产生重叠的目标合法负载，不假设 seqs>1 就必然并发，不要求每次改旋钮都改变命中率。保留待交接引用与 running 集合的区别；引用持有者数量不能简单套 max_num_seqs 上限。

### B. 两 P 与路由

两个 P 有独立请求推进；相同 token 第一次进入另一 P 不得直接借用前一 P 的 KV。某 P 的移除/清空不得破坏另一 P；共享内容在同一个 P 上仍按原生规则复用。验证跨 P 的声明依赖、全局 client 上限、延迟释放以及 P 遍历顺序不会改变语义。

用一个手算可解释的“同域回访 vs 换域回访”小例验证路由影响。缺失映射、错误 P、错 workload 摘要、重试轨迹等反例在原生提交前失败。不要只检查两个 P 都有记录，就宣布多域隔离已验证。

### C. 一次面向最终用途的完整流程

优先复用现有 76 请求或另一个保留完整依赖的小负载，冻结一份覆盖至少两个 P 的路由；结合 actor/session 结构选明确映射，不要求整个 session 只能落一个 P。若只有生成路由，按生成来源标记，不为补真实日志新开实机。

固定 workload、两个 P、routing、client/seqs/MBT、布局与外部协议，运行两个有来源的 KV 预算点。无真实 AFD 预算时，使用清楚标为 assumed 的 low/high 场景，仅验收容量收益分析流程；不能命名为已验证 AFD 实测收益。

完成：校验 → 原生 CPU 运行 → 可比性检查 → 全局/每 P/逐请求差异 → 证据 → 打包 → 只读页面。两档相同也可通过；已有 76 请求没有压力，不为了正收益无限修改它。容量敏感机制复用已有证据；确需新案例时，只增加一个预先估算可解释的短对照，不展开容量网格。

本轮规模性端到端新增运行以这一对为主；其余只做默认兼容与必要短机制例。旧 425 全量不例行重跑，不扫全部并发/MBT/路由组合。每个原生任务保持既有 CPU/内存/墙钟上限；先估算多 P 峰值，不盲目增加进程、池或机器数量。

## 9. 交付和完成标准

交付一个源码/依赖说明齐全的可复跑包、一份 START_HERE、一份验收报告及其必需产物，不再生成内容重复的多份报告。

START_HERE 必须包含真实可执行示例：保留旧默认运行；修改三个旋钮；选择多个 P；导入/生成冻结 route；做两个预算点的 capacity-only；查看逐 P 与逐请求证据；处理输入错误和 incomplete。配置在 CLI/文件中完成，页面继续只读；不建设浏览器调度器或 Web 驱动服务。

验收分别列明：

1. 默认 1 P 回归与 v1 已知修复是否通过。
2. client/seqs/MBT 是否从输入进入原生实例并在输出中如实可见。
3. 多 P 隔离、全局事件推进与冻结落点是否有直接证据。
4. 跨结果可比性、部分失败处理、来源绑定、逐 P 汇总和证据选择是否正确。
5. 新的两预算对照能否独立复跑和查看；预算与 routing 分别是什么真实性级别。
6. AFD 预算映射、实际目标缓存机制及真实时序各验证到哪一步；哪些仍是假设或待适配。

环境允许时实际操作浏览器；已有策略禁止时不绕过，以 DOM 测试和明确的未验收项交付。关键正确性 bug、参数静默失效、跨 P 泄漏、错误比较或来源混淆不得以“有限交付”豁免。

代码测试可做但原生环境不可用时，保留局部结果和最小阻塞，不把 mock/纯后处理通过称为新调度/多 P 已验收。真实预算或路由记录缺失不阻止明确标注的工具流程验收，但必须单列“真实 AFD 对照未完成”。

阶段完成意味着：可以用同一套工具分析不同并发/调度条件和多个缓存域，并在冻结路由下对给定 AFD/non-AFD KV 预算做条件化比较；不意味着已经证明 AFD 实机性能收益。

最终验收报告必须用独立结论明确写出：**工具能力是否完成；本次预算、路由和目标缓存机制分别是真实、推导还是假设；本次是否能够回答真实 AFD/non-AFD 的显存与缓存收益问题。**若答案是否，列出升级结论所需的最小目标部署证据与基线核对，而不是将“尚未具备真实结论”记为本阶段工具失败。

## 10. 操作边界和停止条件

自主进行已有授权范围内的源码核查、隔离环境配置、CPU 测试、离线运行和交付。复用现有工作区与原生环境，遵守 AGENTS.md、服务器健康与 SLURM 规则，不在 head 上长时间计算，不绕过预检/访问策略，不连续换节点重投。

不运行真实模型，不申请新的 GPU/NPU 推理实验，不下载权重，不升级目标引擎，不发 PR/push，不修改线上服务，不删除历史证据。本轮不实现 Ascend 910C backend、AFD FFN/通信执行、完整 D 模型、动态负载路由、基于耗时的 arrival replay、真实 EP 集合同步或性能预测。

达到上述工具验收或清楚记录外部阻塞后结束本阶段。不得为了出现上升曲线、拟合历史、凑“真实 AFD”标签或让全部检查变绿而自行扩展实验。

立即从当前版本与 v1 修复核对开始，然后依次完成：三个旋钮 → 多 P 逻辑推进 → 冻结路由 → 带来源的预算对照与展示。不要重新要求用户选择已确定的路线。

## 实现入口与资料说明

当前包中主要相关入口：`kvsim/native/conditions.py`、`inputs.py`、`capacity.py`、`probe_native.py`、`run.py`、`result_validation.py`、`compare.py`、`prefix_evidence.py`、`bundle.py`、`kvsim/browser/native_results.js`。这些路径用于定位现状，不要求严格维持文件划分；优先小幅复用与拆分，避免复制正式后端。

依据：本次关于六类实验变量、冻结 routing、AFD/non-AFD 最终用途的对话；已交付的 `KVLab_native_v1_20260923.zip`；若可取得，再参考 `KVLab_native_v1_review_20260923.md`。本 Goal 是实施要求，不将制定 Goal 时的文件审查当作原生调度、AFD 或实机验证。具体 AFD/non-AFD 部署与显存预算尚未在本轮证据中冻结，不预设卡数、DP 数或收益数值。
