# Goal v2（最终版）：四张 H20 上的 KV 容量—性能 Benchmark

## 1. 目标与验收原则

在固定模型、同节点四张 H20、原生 P/D 部署和固定 Agent Trace Replay 下，**只改变 P 侧每卡 KV 显存预算**，测量容量对应的本地命中率、本地输入计算计数、后续轮 TTFT 和少量辅助性能指标，形成当前可测范围内的经验响应关系。

先使用现有数据审查 H/L 的可比性，再安排必要的新回放。**默认争取 12、24、48 GiB 三个可比较的容量点；可比性优先于点数。**不要求正收益、单调曲线、精确拐点、饱和区或完整瓶颈归因。

本任务暂时不讨论 AFD。结果定位为“固定回放规则下的闭环容量—性能 benchmark”，不表述为严格相同 token/block 访问序列下的纯缓存因果实验。

执行本 Goal 时，自主完成边界内的审查、必要小修、正式回放、汇总和资源清理，不停留在方案阶段。最多新增两次完整正式回放，不授权超出该预算的实验。

## 2. 复用已有材料，保留历史结果

本地工作目录：

```text
/Users/zhouziheng/Documents/ChatGPT/replay
```

优先读取 `REPORT.md`、`results/summary.csv`、`evidence/formal-3343`、`evidence/formal-3353` 和 `scripts/`。原 `GOAL.md` 是已完成阶段的范围；原 goal、报告和原始证据均保留，不覆盖。

若发现解析或汇总错误，允许在新阶段目录修正派生统计：对所有可比较运行使用统一口径重算，保留旧值、修正依据和脚本版本。不得只修正有利的点。

远端工作目录及证据入口：

```text
/home/david_cwq/zhouziheng/agent-replay-20260917
/home/david_cwq/zhouziheng/agent-replay-20260917/evidence/pd-capacity-20260919
```

已有正式回放：

| 档位／作业 | P KV 预算／卡 | 本地命中率 | local compute | 后续轮 TTFT P50／P95 |
|---|---:|---:|---:|---:|
| L／3353 | 12 GiB | 89.81% | 710,994 token | 0.662／5.229 秒 |
| H／3343 | 48 GiB | 92.54% | 520,530 token | 0.694／25.689 秒 |

历史报告记录两次均完成 174/174 条请求、输入输出长度匹配、无抢占且已清理资源。以上为查找和审查入口，不能替代本轮对原始数据的审查。

**旧结果必须保留，但是否进入新的主比较，要分别判断执行完整性、缓存计数可用性和延迟可比性。**不因 H 更慢就判无效，也不因全部成功就强制合并。若只适合历史参考或缓存统计比较，明确标注。

## 3. 固定边界

| 项目 | 要求 |
|---|---|
| GPU／部署 | 同节点四张 H20；一个 P 实例 TP2＋一个 D 实例 TP2，即 1P1D |
| 模型 | 现有 `/home/david_cwq/jinghaoyu/models/DeepSeek-V4-Flash`；checkpoint、tokenizer、量化和 dtype 沿用原实验 |
| 运行时／链路 | 复用原实验的 vLLM 0.26.0、Mooncake、proxy、P→D 传输路径、依赖及既有本地修改，不升级环境 |
| 唯一容量变量 | P 每卡 KV 字节预算；以运行时预算和实际 block 数确认生效 |
| D 侧 | KV 预算固定为 48 GiB／卡，其他配置不变 |
| 推理配置 | 保持已有 eager、dtype、上下文上限 81920、MBT 8192、P max_num_seqs=2、D=4 等设置 |
| APC | P 开启，D 关闭；不引入 D→P 跨轮回读或额外 KV offload |
| Replay | 并发 3、seed=0、同 actor 间隔缩放 0.1、原有依赖和顺序、adaptive 构造、prompt shape 及采样／输出控制 |
| 输出 | 保留每条原始输出长度，不强制 output=1 |

负载仍来自 AgentInfer commit `7f304555a8bc504c2c2257db2662e896eeeb5c30` 的：

```text
tests/agentbench/replay/claude_trace_8session_requests.jsonl
```

使用三个完整 session，保留全部 174 条记录及原字段：

- `66fd0455-0715-4b7b-ab93-d2aeb4323268`：65 条。
- `c4d23e13-7f6c-4998-af37-d6a3e8b4067f`：75 条。
- `7aefcc2d-844d-4445-b575-350f065a0a52`：34 条。

不得裁剪、复制 session、更换 trace、增加人工前缀、扫描并发或输出长度、剔除慢请求以追求正收益。不更换模型、connector、执行后端、GPU 数量或拓扑。

以已有启动命令、配置快照、manifest 和 patch 为核对依据；相同版本号不能替代对相关源码／patch 的核对。保留既有客户端 patch，只核对影响本轮的文件和低成本模型标识，不重新散列全部大权重或广泛扫描环境。

## 4. 第一步：只用已有材料做最小可比性审查

先不申请 GPU。完成以下有限检查，出口是决定旧点如何使用和下一次回放测什么；不要求解释每一条慢请求。

先核对已有容量、非容量配置、清缓存证据、请求覆盖、实际长度和正式窗口计数。执行完整性、缓存统计可用性和延迟可比性分别判断，不用一个总的 valid 标签代替。

1. **尾部请求对齐。**取 H 最慢约 10 条后续请求，按原始 trace ID 对齐 L，并参考 L 自己的尾部请求。整理 session、actor、轮次、输入输出长度、发送时间、TTFT，以及由已有请求起止记录推得的客户端在途数；它不等于 P/D 队列长度。先确认日志时区和时间基准，再对照邻近 P/D JIT、传输日志，判断是否集中于某个时间段或请求形状。时间邻近只作为线索，不能直接认定因果。
2. **阶段指标。**检查现有 metrics 和日志中的 queue、prefill、首 token、KV 传输统计。可使用正式窗口 histogram 的 sum/count 差分比较均值；没有就标为不可得。不把不同阶段的 P95 相加、相减或当成逐请求关键路径，也不把阶段墙钟时间冒称 GPU 计算时间。
3. **续接结构。**核对既有构造规则、采样配置、context/reset/calibration 记录。已有 prompt/token 记录时，检查相邻轮的前缀保留情况；没有记录则明确无法事后验证 token 级等价。ID 和长度一致不是前缀内容相同的充分证据，生成文本不同或请求交错不同也不自动使闭环比较无效。

仅输出一份简短审查结论和必要的对齐表：

- H/L 哪些指标可用于主比较，哪些只保留作历史参考；依据是什么。
- 是否存在明确、可用小修解决的运行干扰；未知的原因保持未知。
- 下一次回放的容量、测量协议、比较对象和要解决的问题。

不新增 profiler、分布式 tracing、内核插桩或确定性回放框架。不删除慢请求，不从 TTFT 中估算扣除“JIT 时间”。

**可比较不等于已证明稳定。**配置和测量流程一致、未发现实质性干扰证据时，可以纳入同协议下的单次闭环观察；缺少重复实验不自动意味着不可比较。存在明确协议差异、采集错误或重大运行干扰时，单列受影响的指标并说明依据。H 更慢、JIT 告警或时间邻近本身不足以排除一次运行；没有告警也不证明完全无干扰。完成上述有限检查后必须作出当前证据下的使用决定，不能无限等待“证明没有噪声”。

## 5. 第二步：按审查结果使用剩余预算

**总计最多四次完整正式回放，已有两次，因此本阶段最多新增两次。预算按回放次数计算，不按新增容量档位计算；复核旧容量也占一次。**

没有可靠的低成本选点依据时，默认新增 **24 GiB／P GPU，即 25769803776 字节**。它是 12 与 48 的几何中点，不是预期拐点。本阶段不扩大 12–48 GiB 范围，不因 12 GiB 命中率接近 90% 就判定容量充裕或自行下探。

| 最小审查结果 | 第一次新增回放 | 剩余一次如何使用 |
|---|---|---|
| 旧 H/L 可纳入同协议观察 | 24 GiB | 通常不用，只有明确异常或新增信息需求才执行 |
| 一个旧点有明确、可修正的干扰，另一个仍可比较 | 复核问题容量 | 先审查复核结果；协议可比较时考虑补 24 GiB，否则优先补齐修正协议下的成对对照 |
| 修正协议后两个旧点都难以比较 | 修正协议下的一个端点 | 同协议下的另一个端点，形成 L′/H′；不强求三点 |
| 无明确修正办法，但具体尾部异常是否再次出现会影响结论 | 有明确判别问题时，针对性复核该容量，通常为 H | 依据结果决定补 24 GiB、补齐成对对照或停止，不自动追加 |

若只是未评估运行间波动，没有具体重大干扰疑问，按第一行补 24 GiB。若只能可靠比较缓存统计，新增点必须有明确的缓存分析价值，并将延迟响应标为未判定，不以凑满三点作为理由。

每次新增正式回放前，在现有台账记录容量、协议、比较对象和待回答问题。针对异常的复核还须事先写清：该现象再次出现或不再出现，分别会怎样改变旧点的使用方式和下一步决策。不能仅因 H 的 P95 不理想就重跑。**第一次结束后再决定第二次用途，不提前机械跑满。**

**复核不得只选较快的一次。**同协议下所有有效运行逐次展示；只有独立的错误或干扰证据才能改变比较用途，不能按性能好坏剔除。一次复核能提供异常是否再次出现的证据，不能证明系统已经稳定。

完整 174 条负载一旦启动，即计入次数，包括中途失败。仅启动失败、尚未调用完整 Replay，不占正式次数，但启动修复须有诊断依据和有限重试，不能变成无界调优。短检查不得变相运行完整负载或绕开次数预算。

## 6. 测量协议

复用已有脚本和输出结构，只做必要小修。正式运行前明确记录所采用的预热、编译缓存准备、P 前缀清空和计时流程。

- 优先沿用原协议。仅在发现明确干扰时，才增加少量有针对性的预热；不使用完整正式 trace 充当免费预热。
- 区分 JIT／运行时预热与 P 前缀缓存状态。正式开始前等待在途请求及传输完成，确认 P 前缀清空成功，再采集起始指标。
- 若修改了会影响性能的协议，明确哪些旧结果仍可比较；不能默认将新旧协议点混入同一延迟趋势。统一编译缓存准备不等于已经消除了所有编译或顺序影响。
- 容量切换沿用现有安全重启方式；不开发重连机制。上下文上限保持不变，不为低容量点缩短最长请求。
- 保存正式窗口前后 P/D metrics、全部逐请求结果、JIT 和传输日志，以及实际容量与启动命令。

正式终止快照在全部正式请求和传输收尾后采集。每次启动仍须确认服务健康、实际容量、固定配置与缓存状态。使用原有线性插值分位数算法，保持统计口径一致。

不重复已经完成的整套 smoke。只有暴露出容量参数未生效、APC 异常或采集错误时，才用几条请求排障。

首次申请 GPU 前，记录有限的启动排障时间预算和重试上限，不因失败自动续期。同一故障在一次有依据的修正后仍复现，不再自动重复启动；先依据已有日志重新诊断。只有发现新的明确原因和小修方案，且仍在预设预算内，才允许再次尝试。达到预算或需要扩大范围时保留证据、报告未完成项。服务器健康规则始终优先。

## 7. 指标与解释

| 类别 | 必须交付的内容 |
|---|---|
| 容量 | P 每卡配置并经运行时确认的 KV 预算 GiB、实际 block 数 |
| 缓存／输入 | 正式窗口 P local hit/query、token 加权命中率、local compute token 计数及其来源 |
| 主要延迟 | 后续轮 TTFT P50/P95 和样本量；首次／独立／reset 请求单列 |
| 辅助性能 | 复用已有客户端 E2E P50/P95、回放完成时间，写明统计样本和时间窗口 |
| 有效性 | 计划／成功／失败／超时／未发送数、实际输入输出长度、P/D 抢占、传输错误 |
| 低成本诊断 | 已有 queue/prefill 等阶段统计、JIT 记录；缺失明确标注 |

保持原有 TTFT 定义：客户端发出请求至首个有效生成内容，不含人为等待或空流事件。按上下文续接链及实际 reset 划分请求，保留原 trace ID、session、actor 和轮次。

命中率不混入 D 或外部 KV，不重复累加 TP。明确原始 counter、标签、时间窗口和直接／推导属性；分子分母使用一致口径，来源计数、缓存查询计数和阶段统计不能直接混用。local compute 是输入 token 来源计数，不等同 GPU FLOPs、GPU 时间或全部重复计算。只有输入来源与窗口一致时，才使用相应守恒关系；hit 增加和 local compute 减少不作为两份独立的 GPU 耗时证据。全部请求的计算计数不能无依据分摊给后续轮。

阶段 histogram 只在口径一致时用 sum/count 差分比较均值。只有起止 gauge 快照时，不推断全过程峰值、平均队列或平均 KV 占用，也不为补齐这些指标新建采集系统。

命中变化用百分点；TTFT 改善率为 `(低容量 TTFT−高容量 TTFT)/低容量 TTFT`，百分数展示时乘 100%，同时保留绝对值。失败与缺失显式展示，不用成功样本分位数掩盖不可运行。

不将闭环完成速率解释为最大服务吞吐；不凭高命中率或零抢占判定无淘汰、无容量压力；未观察到 TTFT 改善不等于已证明瓶颈在缓存之外。容量增大后命中与延迟均不保证在单次闭环运行中单调。

## 8. 不作为前置或验收的工作

默认不做离线容量模拟、独立人工压力 benchmark、精确工作集、归一化压力横轴、常规多次重复、完整瓶颈归因。

若已有 block 记录及工具可以直接使用，且其输出会改变下一步选点，可选用作参考；否则跳过，不新增适配或校验工程，不阻塞实测。模拟值不能混入实测图。

不追求逐 tensor 精确物理字节合计，不将 TP 两卡显存相加当作等倍逻辑 token 容量。不扩展到 AFD、单体与 P/D 对比、offload、路由或淘汰策略研究。

## 9. 交付与完成标准

在独立的新阶段结果目录保存，保留全部历史产物：

1. 一份报告：先回答容量响应和证据边界，再说明旧点的使用决定及有限异常审查结果。
2. 一张汇总表和 `summary.csv`：包含容量、命中、local compute、TTFT、E2E、回放时长和有效性；每个点标注运行编号、协议及是否进入主比较。
3. 两张主图：容量—P 本地命中率；容量—后续轮 TTFT P50/P95。仅画实测点；同容量复核显示多个运行点，不可比较的协议分面或分组，不用同一趋势线暗示可比。不合并多次请求后冒充一次运行分位数，不绘制没有重复实验依据的置信区间。
4. 逐请求数据、尾部对齐表、配置与协议、启动和回放命令、运行时／模型／trace 标识、指标快照、日志及资源清理证据。复用已有输出，不另搭报告系统。

**完成标准：**完成最小审查，按其结论执行有明确用途的新增回放（通常先一次），在剩余预算内取得可解释的容量响应观察，完成上述交付与资源释放。默认争取三个可比较容量点；若协议修正需要成对复核，两个新的可比点即可，不强求三点。

报告必须回答：测了哪些容量？本地复用与输入计数如何变化？后续轮 TTFT 和 E2E 如何变化？哪些点、哪些指标可直接比较？证据是否足以讨论容量敏感性或边际递减？不足时明确不判定拐点、饱和或瓶颈。

每档单次结果是工程观察；174 条请求不是 174 次独立系统重复。延迟比较成立但原因未判定，可以完成工程 benchmark 并明确归因限制；如果连延迟比较条件都不能可靠建立，则标为“缓存观察完成、延迟响应未判定”，按部分完成报告，不能宣告全部目标达成。可比较与稳定性、归因是不同要求。

若资源或核心兼容性阻塞，或次数耗尽仍无法形成所需对照，报告部分完成、具体证据和未完成项；不把准备完成当作 benchmark 完成。若所有新增实验均没有明确用途，可停止并交付审查结果，但应标为部分完成、明确未扩展出新的容量 benchmark。预算用尽后不自行追加实验。

## 10. 执行与资源安全

执行任务时，边界内的审查、常规操作和必要小修自主完成，不逐步请求确认。若需要突破四卡、固定负载、既定部署或最多新增两次完整回放，停止扩展并说明原因。

遵守现有全局服务器健康和 H20 SLURM 规则及已授权 reconnect 例外。通过本地系统 `ssh vllm-h20-head` 协调，保持 Codex 持久远程连接关闭；所有计算和模型加载在有效 SLURM 分配内执行。使用明确时间、资源和重试上限，不重复提交存活作业，不干预他人或未归属的同账号进程。

显式申请 SLURM partition、GPU 和 wall time，不改写调度器设置的 `CUDA_VISIBLE_DEVICES`，不扩张既有授权例外，不做无差别 kill 或全局清理。

每次保存运行前状态，跟踪本轮作业、PID、端口和输出目录；结束时在分配内停止服务并核验 GPU、进程、IPC/共享内存和端口，再从控制节点确认调度终态。只有清理和结果保存都完成后，才能宣告该运行完成。
