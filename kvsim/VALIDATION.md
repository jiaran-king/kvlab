> 2026-09-22 更正：此前竞争案例的容量台阶受错误回收队列影响，相关容量收益结论失效。修正后所有已复测容量点均为31.9534%，有效缓存替换为0。详见 kvsim/ASSETS-v03.md 与 output/kvsim/v03/reclaim-correction.json。以下旧结果仅作历史记录。

# 单文件版迁移验证（0.2）

2026-09-22：网页计算迁移到 `browser/core.js`，与 Node CLI 共用。`KVLab.html` 内嵌核心和 425 计划；构建检查确认无 fetch、API 请求或外部脚本/样式。

16 组完整结构对照：三种 block size × 四种容量（0.01/1/8/24 GiB），另含默认案例、425 案例，以及两组超过 81,920 token 的输入（未设置/显式设置部署上限）。比较场景生成、逐请求命中/计算/解释、失败位置、每 P 状态和全部占用时间线，忽略版本号，其他字段严格一致。记录：`../output/kvsim/verification/browser-core-tests.txt`。

当前限制：自动化浏览器拒绝 file:// 导航，未绕过该策略。因此新单文件的真实浏览器 Worker、下载和导入交互尚待用户在系统浏览器确认；以下旧 HTTP 界面验收不代表新版本的浏览器验收。此前 Failed to fetch 的服务依赖已从新网页移除。

---

# 首版验证记录

日期：2026-09-22。所有计算仅在本机 CPU 上执行，无 GPU/NPU、无模型服务。

## 核心

`python3 -m unittest discover -s kvsim/tests -v`：18/18 通过。覆盖内容身份、同内容跨session、跨域隔离、末token边界、D输出不自动缓存、reset内容不清空域、重复hash独立物理页、活动引用、释放后复用、低容量与预算不可行、未完成状态不可复用、场景重跑、实际token、425结构、原源码block表与retention mask，以及多组稀疏恢复需要反复收缩的反例。

原始输出：`../output/kvsim/verification/tests.txt`。

## 浏览器

用 Codex 内嵌浏览器实际操作本地页面：默认运行；容量16→12后的待重算提示；轮数4→5；4/8/12容量扫描（4GiB不可行、8/12成功）；逐请求解释；导出scenario.json到Downloads；重新导入并运行；切换计算量曲线；查看各P汇总。最终浏览器控制台无错误。

默认16请求：总P输入375,296，采用196,608，计算178,688，采用比例52.38744884%。网页导出场景在网页重跑与CLI重跑后均一致。文件：`../output/kvsim/verification/browser-scenario.json`、`browser-roundtrip.json`，完整重跑在`../output/kvsim/browser-roundtrip/`。

内嵌浏览器不产生Blob下载，已改为本地标准Content-Disposition附件。macOS不接受数据段setrlimit，已改为CPU时间上限＋事件边界RSS检查＋父进程超时收尾。

## 425案例

`python3 -m kvsim run --config kvsim/cases/replay425.json --output output/kvsim/replay425-final`

425/425完成；计划输入13,707,996。当前16GiB/16K、合成内容＋虚拟轨迹下采用8,962,048，计算4,745,948（65.3783%）。这些是本场景的模拟值，不是历史Ascend实验复现值，也不与H20结果拼接。历史Ascend实际token、chunk/释放时序、完整运行时补丁缺失；规则验证和输入结构检查已经完成，严格逐请求历史数值验证尚不可声称完成。

完整来源、范围与假设见RULES.md。可在拿到这些资产后使用相同的输入契约及独立compare_observed接口继续验证，不需要改缓存引擎以拟合结果。

## 本地服务

只监听127.0.0.1:8765，有明确到期时间，单CPU工作进程；首次验证服务已关闭；第二次服务按末尾记录保留。健康、归属和清理记录在`../output/kvsim/verification/health-*.json`。启动前已存在的PID1041僵尸归属Clash Verge父进程1012、始于9月13日，未触碰。

## 默认场景52.39%平台期核对

用户在页面提出8→24GiB采用比例相同后，重新对当前默认场景核对：4链/4P，各链4轮，输入20000、22304、24608、26912。各链采用0、16384、16384、16384；总采用196608 / 总输入375296 = 52.38744884%。8GiB每域2643物理块、24GiB每域7929块；8GiB全场72次物理替换，24GiB为0，但实际采用量完全一致。这说明预算生效且替换状态不同；所需16K边界仍可恢复，不能把物理替换数直接当作丢失复用token。

当前用户正在浏览本地页面，因此第二次服务保持其900秒自动退出期限；没有宣称已关闭。最终就绪健康与精确PID归属记录见health-ready.json。先前重启的服务已完成清理检查。

本次设置调整：新增对明确部署上限拒绝、资源超限分类、负载预览计数的断言；104,352 token 请求完整执行，计算量守恒。页面脚本语法检查通过。真实 file:// 交互验收限制不变。
