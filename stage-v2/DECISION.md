# 最小审查与第3次正式回放决定

H/L 执行完整，174条均成功、长度匹配、reset成功；既有配置比较仅P预算不同。缓存counter仍可使用。原始记录和派生统计均保留，没有删除慢请求。

H P日志有42组配对的TileLang编译起止记录，L没有；H最慢10条后续请求的发送—首token区间全部与至少一组编译区间重叠。42组在两个TP rank上重叠，其388 rank-seconds合计不能当墙钟延迟，也不能从TTFT扣除。日志行有缓冲，按记录内时间而非行顺序对齐；UTC客户端与UTC+8服务日志匹配到秒，不能声称亚秒级精准归因。JIT warning不是完整编译计数；L仍有JIT监测告警。

正式窗口histogram的sum/count差分各174请求：P prefill均值H2.6785s/L0.9233s，Pqueue0.3378/0.2252s，Dqueue4.8934/1.2911s，Ddecode16.4035/16.7530s。它们不是独立可相加阶段；D排队可能含等待P/KV，不能据此认定D本身是瓶颈。客户端在途数仅由HTTP窗口估计。

两档adaptive记录都是171pad、3trim、0reset，174exact；没有保存完整正式prompt/token，不能事后证明token级前缀等价。结构规则和计划一致，没有已记录的reset破坏，但这一限制保留。现成日志足够作出下一步决定，不开发模拟器或tracing。

## 下一次：H复核（H2），48GiB/卡

问题：使用现有已经历H/L的磁盘编译缓存，原有相同两条预热+P清缓存协议下，H的编译区间和尾延迟是否再次出现？不新增预热形状、不改变服务或Replay参数，不清理磁盘编译缓存。旧H标为有可识别编译干扰的历史延迟观察，保留其缓存统计和所有原始延迟数字；H2即使更快也不替换旧H。

若H2没有相同编译干扰且与L的测量条件一致，L/H2作为缓存准备状态更接近的一组单次工程观察，剩余一次优先测24GiB；旧H在图中单列。若编译或其他重大干扰复现，不自动补24，先依据H2结果决定是否存在同预算内可比较的下一点；证据不足时报告限制或部分完成。任何结果都不证明运行已稳定；不因H2数值更差而删除它。

正式总预算4，当前已有2。启动排障本阶段上限45分钟、最多3次服务启动尝试；每次需要具体修正依据，同一故障修正后复现先诊断，不能盲目重启或自动续期。每个正式作业仍用原SLURM2小时上限、内部服务/Replay超时。调度等待不触发重复提交。第4次用途在第3次完成后决定。

## H2 完成后的第4次决定：M 24 GiB

H2/3384 完成174/174，长度全部匹配，P命中6457088/6977618=92.5400%，local_compute520530，与旧H完全相同。后续161条TTFT P50/P95为0.698857/3.948551秒；E2E P50/P95为11.528303/62.596015秒。正式窗口P/D均无TileLang compile begin记录，P仍有2条JIT warning，不能称完全无编译。P105条传输统计均无非零错误；D无独立此类日志，标不可得。

H2与L的服务命令除P预算外一致，Replay配置除输出目录外一致。SLURM分配了同节点另外四张同型号同容量GPU；GPU UUID未固定，作为工程观察的限制披露，不声称完全相同物理设备或独占主机。H2 P/D/controller无剩余进程、实际tracked PID已不在after快照，GPU apps空、端口与IPC不变、无新增shm名称，仅既有系统监测文件元数据变化；available memory约2TB且存储/负载正常。调度终态COMPLETED0:0。

按事先规则，L与H2纳入缓存准备状态更接近的单次工程比较，旧H保留并单列其编译活动；不是按性能好坏选择快点。下一次且最后一次为M：P25769803776字节/卡，D51539607552字节/卡，其余协议不变。比较对象L和H2，问题是12→24与24→48区间的命中增量、本地输入计算及延迟响应是否不同；不要求单调或拐点。沿用原两条预热、既有磁盘编译缓存和P清空，不增加任何形状。该次启动完整Replay即占第4次；无论结果如何不再新增完整回放。若M出现新干扰则如实分组和标注，不自动再跑。
