# 425 条固定负载调整

按用户指示，不重跑 12 GiB；后续 48、24 GiB 使用 `successful-425-trace.jsonl` 中冻结的相同 425 个原始请求。原 Goal 保留，本文件记录执行范围调整。

主对照使用五个完整 session 与第六个 session 的 97 条请求。原始相对顺序、输入输出长度、actor 与 send_after/context_after 依赖保留，依赖闭合已验证。请求并发仍为 3；未来两档统一 session 时限 5400 秒，HTTP 操作超时仍为 900 秒。固定 seed 不保证闭环实际交错或生成内容完全一致。

12 GiB 原运行计划 467 条，提交 426 条，成功 425 条；1 条提交后因 3600 秒 session 时限取消，41 条未提交，没有记录到 HTTP 错误结果。保留全部原始证据，不将计划覆盖改写为 425/425。

取消请求已执行 prefill，服务端输入来源计数包含其 36,088 个输入 token。长输出生成可能影响并行成功请求的排队、缓存与延迟；事后筛选成功 ID 不能消除这些运行影响。12 GiB 的成功样本统计与整场缓存指标单独展示，不与新两档构成完全可比的三点曲线。

主问题为固定子集上 24→48 GiB 的容量响应。子集在 12 GiB 运行后确定，排除了长输出及后续链路，不能代表完整六-session 负载，也不能同等可靠地定位三档之间的拐点。单次运行报告观察值，不虚构误差条或把小差异称为稳定收益。

继续既有 48→24 顺序，不增补 12 GiB，不扩大到八个 session。保留原运行尝试预算、观测口径和服务器健康要求。
