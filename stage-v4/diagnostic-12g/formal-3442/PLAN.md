# 12 GiB 定向可靠性复核（L3）

用户2026-09-20授权一次补测。仅运行新425计划的12 GiB一次，D48 GiB，P2/D4 max_num_seqs，客户端并发3；保留原seed、输入输出、等待比例、超时、预热/reset和事件配置。新增观测不更改模型、缓存算法或调度决策。

目的：复核L2低复用和长尾是否再次出现；将本次额外query与逐请求重复查询、分配失败、connector推迟及其他未调度分支对齐。只记录调度器发出的token数量，不能冒称测量了GPU FLOPs。

实施：P进程专用PYTHONPATH导入钩子，包装原函数但保持参数和返回值；按查询/分配失败记录缓冲JSONL，不输出token内容。2万条诊断事件上限，600个请求ID上限，超过即报错；单次2小时SLURM，无自动重试。预热期间验证hook生效，正式前清P缓存。完整周期metrics留服务器，不下载。

旧新差异：此前已确认旧467计划与新425计划的派生seed及初始合成文本变化，不能将L2视为旧L逐字重复。离线检查首次明显时延分歧相对旧长输出请求入场时间，检验长输出占用并发是否足以解释早期差异。

验收：425/425、计划长度和设备/配置对照；逐请求query求和与P counter增量对齐；重复查询按未准入原因分组；输入来源缓存占比、local_compute、prefill、TTFT和任务窗口与L2对照。单次复测不要求数值相等，也不保证完成所有归因。不自动重跑24/48。

文件隔离：本目录使用独立一次性运行账本、启动副本与诊断钩子，不修改既有正式脚本或已安装vLLM。原始证据保留。
