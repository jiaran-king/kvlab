# 夜间主动推进：v5研究实验

最高优先的当前用户授权（2026-09-20约21:39）：用户睡眠期间由定时任务驱动“检查→诊断→修复→验证→提交受影响阶段→再检查”；有效运行等待下一次唤醒，不因一次脚本/观测故障就停止并要求用户确认。目标是完成有效C12与F12/F24配对及分析，而非达到特定性能数值。本文覆盖旧HANDOFF/PLAN中“不自动修复、失败立即停用”的运维条款。

## 当前状态

旧C12/3454在预热后观测验收失败，原F12/3455、F24/3456取消。现场保留stage-v5/formal-3454，旧报告stage-v5/REPORT.md。

已确认并修复日志刷新缺口：旧写入依赖下一次emit触发flush；新实现每个查询决策批次追加query_batch_committed并flush后才返回调度函数。新旧代码跨进程测试确认旧记录可能不可见、新两条可见，os._exit不影响可见性；完整返回对象/metadata与原调用次数验证通过。

新目录：本地stage-v5/retry-1；远端/home/david_cwq/zhouziheng/agent-replay-20260917/evidence/pd-capacity-20260919/stage-v5/retry-1。
新作业：C12 3458 → F12 3459 → F24 3460；CPU终态记录3461。2026-09-20 21:38:58提交，afterok串联，阶段退出前验收成功才放行。每GPU作业4H20、32CPU、512GiB、2小时，原425协议不变。

## 每次唤醒要做什么

1. 阅读本文件、retry-1/REPAIR.md及最新submission/repair-state，不能反复检查已经结束的旧3454。仅通过系统ssh vllm-h20-head轻量协调，遵守SLURM，不启用persistent SSH。
2. 检查当前作业终态/服务ready/观测smoke/请求进度及日志错误。证据完整、持续推进则不打扰用户，等待下一次定时任务。不是全程追日志。
3. 失败或明显不符合协议时，主动获取必要小证据，诊断具体原因；在不改变科研协议的前提下修复观测、序列化、启动/回放脚本或验收工具。先做能覆盖该故障的CPU验证，再提交必要阶段或作业内短链。不通过降低验收标准、改输入/并发/超时/容量/缓存算法来使其通过。
4. 优先复用已有完整数据：仅验收工具错，先修复并重新验收，不重复GPU。有效C12捕获可在符合版本/语义约定时用于后续固定输入；F12/F24执行与观测版本必须一致，不能混入不兼容旧点。修复影响原执行/观测时创建新retry目录，保留旧现场，重新建立必要比较。
5. 不在活动服务上热改代码。先确认失败服务清理、依赖作业释放/取消，再修复并提交；仅处理本链确切资源。每次修复更新repair-state.json，列原因、改动、验证、复用/重跑理由及新作业ID。
6. 发生一次可修复故障不关闭本自动化，不等待用户逐阶段确认。修复后简要通知；正常无变化保持安静。

## 资源和停止边界

本夜使用一次监督流程，每个GPU作业有2小时硬上限，无无限重启脚本。最多4轮后续自动修复、包括当前重提在内最多10次实际启动GPU作业，取消未启动的依赖不计；这些是防止失控的上限，不是要用满的目标。达到上限先继续可完成的CPU诊断/已有数据验收，报告具体剩余问题，不编造实验完成。

2026-09-21北京时间09:30后不再新提交GPU作业，已经健康运行的作业在原2小时上限内完成。调度器/存储/主机健康异常仍需保护服务器；不得绕过调度器、重启系统服务或无限重连。未知底层故障不能用原样重试替代诊断。

这两条安全边界以外，不因为性能收益小、非单调、未复现历史L2，或一次可修复软件故障宣布任务结束。不能承诺一定在明早解决全部机制，但要积极推进有效实验与分析。

## 最终验收与交付

阶段验收口径沿stage-v5/PLAN.md与retry-1/PLAN.md：425覆盖、真实P输入、语义身份、query attempt/group联合查找、最终采用、计数对账。C12与F12/F24协议分开，不将C12充当F12，不以运行间hash相等替代token数组比较。

完成后形成summary.csv、paired-requests.csv、REPORT.md，保留所有正负零缓存差额并对回全场净收益，分析实际LCP与恢复边界、slot等待及发送后TTFT。不能只报告作业成功。历史矩阵缺项已在stage-v5/historical-matrix-supplement.json。

不下载完整周期metrics或大事件文件。需要全事件摘要时使用有界CPU作业；输入token数组约55MB是必要分析资产，可取回。head只做轻量协调。终态及资源清理必须验收。

只有实验与分析完成，或上述明确资源/安全边界无法继续，才停用自动化并报告。旧自动化失败即停用条款作废。

用户补充：实验成功且验收、结果汇总完成后，立即将h20-v5设为PAUSED，通知结果及定时任务已关闭，不再重复唤醒或实验。

## 22:44自动修复续跑（最新）

C12/3458 425/425、完整token/查询/采用对账通过，22:32:18 COMPLETED/0:0，cleanup-audit通过。F12/3459在前驱退出同秒开始，6秒后最早端口bind检查报EADDRINUSE；未启动任何服务，captured ports-before无实验监听，controller children为空。F24/3460自动取消。时间线与无监听快照支持前驱连接TIME_WAIT解释，但未保存当时TIME_WAIT表，不能声明历史直接实测。

新增wait_ports.py仅在原controller启动前运行：原bind失败时核对ss，无listener且明确TIME_WAIT才等，最多120秒；真实监听或未知占用立即报错。测试覆盖TIME_WAIT释放、真实监听拒绝及超时上限。**没有修改pd_controller.py/pd_service.py/client/hook，不影响C12兼容性，也不改变HTTP超时。** 只改SLURM前置流程与CPU终态收集器。

已复用C12捕获，续提F12/3462→F24/3463，终态CPU3464。提交清单retry-1/resumed-1.json，终态将写scheduler-terminal-resumed-1.json。followup_repair_cycles_used=1；已启动GPU分配3458、3459，此后查新3462实际状态。无需重跑C12。

head发现64个已归属Codex reconnect D残留，完成一次精确SIGTERM并记录known-residue-cleanup-resume.json，不声明全部已回收，不重复清理；SLURM/storage响应正常，按授权例外继续，仍要求计算节点gate通过。

## 23:41阶段检查
F12/3462完成425/425，ACCEPTED passed/frozen_input_verified=true，adopted=11,136,256，query_calls=448，exit-code=0且cleanup-audit通过。F24/3463已按afterok:3462自动启动，计算节点health-gate通过，未见实验错误。此时3462的scontrol记录已被清除（sacct未启用）；其终态依据保存的退出码/验收/清理及afterok后继实际运行，不能声称持有直接scontrol COMPLETED快照。勿因这项记录保留期问题重跑GPU。

## 00:24检查与本地初步数据
F24/3463运行47:55，421条完成记录，stderr无错误，仍正常推进。stage-v5/preliminary/已取回C12/F12完整小型请求证据与prom端点、F24在00:19的393条完成快照及当时query/proxy快照。传输session23136已结束；F24文件是运行中快照，不能当最终完整产物。preliminary/summary.json与summarize.py生成：匹配393条子集F12/F24输入缓存占比79.543%/90.094%，local_compute 2546243/1232963，续接TTFTP95 8.410/2.911秒。正式报告须用最终425配对覆盖，不能沿用这个子集。
