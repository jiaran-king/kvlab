# F12-A / F12-B exact 同容量复用复核

## 协议有效性

两次运行均为 **12 GiB P 侧 KV 预算、425 条冻结请求、同一 P/D 配置和同一观测实现**。F12-B（3479）使用与 F12-A（3462）相同的 source identity；两次均通过完整输入、计数对账和资源清理验收。3473 是此前使用额外诊断 flush 的失败运行，单独保留，不进入本对照。

source identity 配对检查结果：`True`。

## 汇总结果

| 指标 | F12-A | F12-B exact |
|---|---:|---:|
| 请求 | 425/425 | 425/425 |
| 输入缓存占比 | 81.2391% | 81.2373% |
| 查询命中率 | 77.1602% | 77.3307% |
| 最终采用本地缓存 token | 11,136,256 | 11,136,000 |
| 本地输入计算 token | 2,571,740 | 2,571,996 |
| P prefill 均值 | 1.225 s | 1.219 s |
| 后续轮 TTFT P50/P95 | 0.637/7.987 s | 0.731/7.996 s |
| 请求 E2E P50/P95 | 11.176/64.598 s | 11.474/65.947 s |
| 正式任务窗口 | 2839.2 s | 2894.4 s |
| KVEvent 移除条目 | 217,750 | 216,621 |

KVEvent 的移除条目是 hash 条目统计，不能直接解释为容量驱逐数。两次事件流均完整且无 gap。

## 逐请求差异

paired-requests.csv 保留全部 425 条配对请求。F12-B 相比 F12-A 的最终采用缓存差额为 -256 token：0 条增加、1 条减少、424 条不变。缓存采用量只在一个请求上少 256 token，未出现大范围复用分叉。

发送后 TTFT 的逐请求差异中，P50 差额为 0.021 s，P95 差额为 0.149 s；E2E 差额的 P50/P95 为 0.309/1.882 s。任务窗口存在约 55.2 s 的运行间差异，但没有对应的缓存采用量差异。

## 结论

在观测实现一致、输入和容量一致的 F12-A/F12-B 对照中，未观察到历史 L2 那种大范围缓存复用退化。此前 3473 的 ready 超时没有在 3479 重现；它应作为一次使用额外诊断 flush 的 P/D 协议失败样本保留，不能作为稳定的 12 GiB 性能点。

这组结果支持：**当前 12 GiB 配置在这份冻结负载上的缓存采用行为可以在两次运行间基本复现；历史 L2 的异常不能仅由“12 GiB 容量不足”解释。** 这不等于已经找到了历史 L2 的根因，也不把两次运行外推为所有负载的稳定性证明。

## 目标请求的查找证据

F12-A/F12-B exact 只有一个请求出现最终采用量差异：`r00000372-1c6acbc472187251`。两次实际 P 输入均为 44,410 token，语义缓存身份字段一致；根据保存的输入 token 数组，它们与同一 `context_after` 前驱 `r00000370-1c7ab7abe2c3cb06` 的公共前缀均为 43,434 token。

该请求在两次运行中都只有一次查找尝试并正常分配。F12-A 中 group 0 返回 256，后续滑窗 group 均返回 256，联合结果和最终采用量为 256。F12-B 中 group 0 仍返回 256，但 `[1,2]` 及后续滑窗 group 返回 0，联合结果和最终采用量为 0。详细边界见 `target-state-summary.csv` 和 `target-state-summary.json`。

因此，这个单点差异出现在相同输入复用机会下的分组查找／联合采用结果；现有事件摘要没有请求归属或 block 查找身份，不能进一步判定底层状态未保留、恢复条件不满足或其他运行时状态差异。差额只有 256 token，不能解释历史 L2 的大范围退化。

## 失败运行 3473 的部分指标

3473 使用了额外的诊断 flush，且覆盖不完整，因此不进入 F12-A/F12-B 性能对照。正式窗口的已有计数摘要仍可作为故障记录：423 个成功请求的输入为 13,637,190 token，最终采用本地缓存 10,289,152 token，输入缓存占比 75.45%；查询 token 14,504,083、查询命中 10,289,408，查询命中率 70.94%；本地输入计算 3,348,038 token；P prefill 均值 1.617 s；后续轮 TTFT P50/P95 约 0.733/10.885 s；请求 E2E P50/P95 约 11.741/65.738 s。由于 1 个请求失败、1 个依赖请求跳过，这些数值只描述成功子集及正式窗口，不能作为完整 425 请求的性能点。完整故障链和覆盖范围见 `FAILURE-3473-REPORT.md`。

## 复算入口

配对汇总和逐请求差异可由 `analyze_repeat.py` 复算；最终汇总报告可由 `make_final_report.py` 生成。目标请求的输入数组、`context_after` 公共前缀和分组查找摘要由 `target_state_summary.py` 在有界 SLURM CPU 作业中生成。所有脚本均只读取已有运行证据，不调用模型、不提交 GPU 作业。
