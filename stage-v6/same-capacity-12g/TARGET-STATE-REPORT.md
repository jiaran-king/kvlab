# 目标请求状态摘要

目标请求 `r00000372-1c6acbc472187251` 是 F12-A/F12-B exact 配对中唯一出现最终采用缓存差额的请求。两次输入均为 44,410 token，语义缓存身份字段一致；从各自运行保存的实际 P 输入中计算，目标输入与 `context_after=r00000370-1c7ab7abe2c3cb06` 的公共前缀均为 43,434 token。

两次运行各只有一次查找尝试。A 的 group 0 返回 256，后续滑窗 group 均返回 256，联合结果为 256，最终采用 256；B 的 group 0 仍返回 256，但 `[1,2]` 及后续滑窗 group 返回 0，联合结果为 0，最终采用 0。两次均正常分配，B 不是因为分配失败而放弃缓存。

这说明该单点差异发生在同一输入机会下的分组查找/联合采用结果；现有事件摘要没有请求归属或 block 查找身份，不能进一步判定是某个状态未保留、恢复条件不满足还是其他运行时状态差异。它只占 256 token，不能解释历史 L2 的大范围退化。原始紧凑摘要见 `target-state-summary.json`，表格见 `target-state-summary.csv`。

| 运行 | target/context_after LCP | group 0 | groups 1-4 | 联合返回 | 最终采用 |
|---|---:|---:|---:|---:|---:|
| F12-A | 43,434 | 256 | 256 | 256 | 256 |
| F12-B exact | 43,434 | 256 | 0 | 0 | 0 |
